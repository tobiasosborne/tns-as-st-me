# Work order — 2+1 wave, lane W4: the 2+1 boundary battery (bd tns-2p1)

The adversarial lane of the 2+1 wave (pattern: G5, whose battery
adjudicated the general-G swarm — imitate theory/soft-index-g-boundary.md
in format and discipline). The other lanes try to transplant the
campaign's theorems to 2D; you map where the transplants MUST fail or
change statement, by computation, before any critic round.

MANDATORY BATTERY (ED or exact algebra; every finding becomes a standing
gate):

1. **Perimeter vs volume.** The 1D window-charge machinery has O(1)
   boundary terms; in 2D they scale with the perimeter. Compute, on a
   small 2D torus, the window-charge commutator norms that the 1D proofs
   bound by constants — establish which estimates survive, which need
   perimeter factors, at what window sizes the difference bites.
2. **No kinks.** The 1D memory observable rides on domain walls with a
   position coordinate (LD-ID: window charge = wall coordinate lift).
   In 2D, domain walls are loops. Compute the 2D analogue of LD-ID's
   identity on a small system: what does the window charge lift? Is
   there a conserved geometric datum (enclosed area? winding?) or
   nothing?
3. **1-form charges.** For toric code on a small torus: the 0-form
   machinery of the campaign applied naively to the 1-form symmetry —
   what breaks? Compute where the current-zero-mode register D10 has no
   2D 1-form analogue (a loop operator has no local current divergence
   in the same sense).
4. **Goldstone counting.** 2D SSB at T=0: does the soft-magnon slope
   statement even have a candidate form? Compute the two-magnon phase
   data on a small 2D ferromagnet patch (the 2D analogue of the
   S2-2body setup) — is there a well-defined 2D "phase slope" datum, or
   does the angular dependence (k is now a vector) make the 1D scalar
   slope the wrong object? This adjudicates whether a 2D soft-value
   corner exists in the current vocabulary.
5. **Register trap in 2D.** Verify whether the full-sector vs
   highest-weight register distinction (ml4_ward_n2_check.py's
   0.00e+00 trap) persists on a 2D lattice ED instance (it should —
   confirm, one number).

Process: PRD.md. You produce DATA and sharp finite statements; every
claim is a computed number with its reproducing command; honest
negatives are the product. HOLDS/FAILS/DEGENERATE/DIFFERENT-STATEMENT
verdicts per item, summary table in the first thirty lines.

READ: PRD.md; theory/soft-index-g-boundary.md (the format exemplar);
theory/ace-ld.md (LD-ID); claims rows LD-ID, M-INDEX-fin, S2-2body,
S-IDX-fin-G; definitions.md D10, D13, D16, D26-D27.

LANE (writable, NOTHING else): theory/boundary-2d.md (CREATE),
theory/checks/boundary_2d_check.py (CREATE: the battery, green exit 0
under python3 -O, no bare asserts, findings incl. negatives encoded as
gates). No edits to existing files. No commits.
