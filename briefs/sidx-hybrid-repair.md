# Work order — general-G HYBRID REPAIR WAVE (the final wave; bd tns-fea)

You are the single repair wave ordered by
`theory/verdicts/sidx-general-g-joint-critic.md`. Execute its REPAIR
ORDER — the nine mechanical changes — EXACTLY as written, producing the
one hybrid shard. After you, the orchestrator verifies mechanically and
promotes; there is NO further hostile round (PRD capped loop). Therefore:
no redesign, no new claims, no scope beyond the nine steps. Where a step
is ambiguous, take the narrowest reading and record the choice in your
response file.

READ IN FULL: PRD.md; theory/verdicts/sidx-general-g-joint-critic.md
(every objection and the nine-step order); the four lane shards
theory/soft-index-g.md, -g2.md, -g4.md, -g-boundary.md; claims row
S-IDX-fin-r2; theory/soft-index-r2.md (FIN registers).

DELIVERABLES:
1. `theory/soft-index-general.md` (CREATE) — the hybrid shard per the
   critic's design: unified statements (operator theorem per the repaired
   G1/G2 text; G4's label theorem with displayed global covariance),
   both registers, the exclusion pointers to G5's gates, the shared
   SU(3) overlap check table (step 8's numbers verbatim), and the two
   verbatim CLAIMS rows from the critic's PROMOTABLE/design section in a
   MERGE PROPOSALS section. The lane shards remain as records; the
   hybrid states it supersedes them for citation purposes.
2. Edits to `theory/soft-index-g.md`, `-g2.md`, `-g4.md` ONLY where a
   numbered step explicitly orders a replacement/restatement (e.g. step
   5's (C-SCAL) replacement in G2; G1's isotypic-block restatement) —
   minimal diffs, nothing else touched.
3. `theory/sidx-hybrid-response.md` (CREATE) — one row per step 1–9:
   what was done, where; plus one row per MAJOR/MINOR/NOTE objection
   with its disposition.
4. Checker: per step 8, REUSE the existing checkers — add the overlap
   numbers as a gate to `theory/checks/soft_index_g_check.py` if and
   only if the critic's step 8 requires a table gate; no new mutation
   modes.

Do NOT edit claims/CLAIMS.md, definitions.md, notation.md, the r2 shard,
the boundary shard, or any verdict. No commits.
