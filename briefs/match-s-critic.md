# Work order — (MATCH-S) EXTERNAL HOSTILE CRITIC (the one round; bd tns-jpd)

Target: theory/proto-lsz-match.md + theory/checks/proto_lsz_match_check.py
— the proposer proof that (MATCH-S) holds on the separated-preparation
subclass (D29-HS-SEP) for every spin-S ferromagnet, feeding the Letter's
§5 limit law. The prover ran an INTERNAL self-critic (4 MAJOR repaired);
that does not count — same context, same author. You are the independent
round. One repair wave follows, then orchestrator adjudication (PRD.md).

READ: PRD.md; briefs/proto-lsz-match-prover.md (its four hard fences —
verify each); theory/soft-index-r2.md §3 + §7.1 (the (MATCH-S) statement
being discharged and the quarantined D29); claims rows S-IDX-spec-r2,
S-IDX-spec-struct-r2, S-IDX-HR-value-r2, S2-2body-S, AC-EX-2M; then the
targets.

ATTACK SURFACES (mandatory; add your own):
1. **Does (D29-HS-SEP) beg the question?** The subclass restricts to
   separated preparations. Determine exactly what work the separation
   hypothesis does: if it quietly assumes the asymptotic identification
   that (PROTO-LSZ)/LSZ was supposed to supply, the "exact match" is a
   tautology and that is FATAL. Recompute the load-bearing step where
   the finite-time readout meets the on-shell multiplier.
2. **"Exact, stronger than o(ε)" is suspiciously strong.** Verify the
   ordered-limit structure: which limit is taken first, is the
   interchange licensed by a displayed hypothesis, and does the claimed
   exactness survive at finite N (where the row measures live) or only
   after a limit that the D29 protocol does not actually take?
3. **Fence 4 (bridge by proof, not fiat).** The shard claims the
   HR-value connection is "now proved on (D29-HS-SEP)". Walk that proof:
   does it construct the identification or re-label S-IDX-HR-value-r2's
   content?
4. **Subclass nonvacuity.** Exhibit (or verify the shard exhibits) at
   least one concrete member of (D29-HS-SEP) at finite parameters — a
   subclass theorem over a possibly-empty class is the campaign's known
   failure mode (frozen S_W was provably empty; do not let it recur).
5. **The internal critic's repairs.** All four MAJOR repairs are
   unreviewed: recompute each repaired step.
6. **Checker honesty.** Green + all five reds yourself; each red must
   mutate something a real defect produces; the frozen-probe quoting
   must be P1+P2(a)-only; the S2-2body-S interface must consume only
   what that row proves.
7. **Merge-proposal audit.** The proposed bridge row's cell text vs what
   ⟨1⟩10 proves; silent strengthening in either direction.

LANE (writable, NOTHING else): theory/verdicts/match-s-r1.md (CREATE).
Verdict line first (PASS/FAIL(ids), counts); objections with severity,
quoted target, recomputation; PROMOTABLE NOW with verbatim cells if
licensed; REPAIR ORDER. No other edits. No commits.
