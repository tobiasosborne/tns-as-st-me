# Proof Atlas audition critic — round 1

## 1. Verdict

Not yet, but one repair round can make this forwardable. The wow lands twice: first when the same balanced run makes the absolute correlator difference die while the calibrated susceptibility settles, and then when D32/D33 turns that apparent contradiction into a precise statement about a decaying ruler. The pullback can be the distinctive format-level payoff, because the missing dependency edge is unusually honest and useful. Trust currently leaks just before those payoffs: stop 01 overstates the final-value theorem, stop 04 calls a faithful 31-leaf compression “the proof ... in full,” the browser claims to refit a DC intercept that it actually reads from the stored answer, and stop 05 plots its strongest late-time samples beyond the declared finite-`C` proxy without marking that boundary. The route also makes the reader traverse too much proof and review-process material before the decisive two-panel asymmetry. Fix those points and lead the pullback with the physics question the symmetry cluster is meant to answer; the underlying scientific architecture is strong enough to carry the audition.

## 2. Truth-constraint audit — must fix

1. **The final-value hinge is stated as an implication that is false without its hypotheses** (`proof-atlas.html:677-689`, compounded by the universal “three facts ... are one fact” at `:653-658`). A simple pole at zero frequency does not by itself make a response settle: other real-axis poles or nondecaying pieces can prevent the late limit. The displayed identity also needs the transform/distribution convention and a response for which the late limit exists. Replace the prose and display with the actual licensed special case: “For the synchronized retarded response derived at stop 04, `\tilde\varepsilon_sub(\omega)=i A_*/(\omega+i0)+O(1)` and there are no additional nondecaying physical homogeneous components; hence `\lim_{u\to\infty}\varepsilon_sub(u)=A_* = \lim_{\omega\to0}[-i\omega\tilde\varepsilon_sub(\omega)]`.” Describe the QED/gravity triangle as an equivalence under its standard asymptotic and Ward-identity hypotheses, not literally as three facts being one fact.

2. **The microscopic on-ramp changes the system** (`:705-710`). “Two coupled SYK dots” conventionally denotes an inter-dot Hamiltonian coupling, but the ground-truth specification says `H_L \otimes H_R`, TFD preparation, Hamiltonian evolution, and a compact source acting on the right dot (`observables-spec.md:11-13,20-40`); this audition supplies no inter-dot coupling term. Replace with: “two SYK copies/dots prepared in a thermofield-double state, with a compact source acting on the right copy.” If an actual coupling is intended, display and source its Hamiltonian; otherwise do not import the coupled-SYK model.

3. **D32 comparability is promoted into a claim that an inadmissible comparison “measures nothing”** (`:987-998`). Such a difference may be measurable; it simply cannot license *memory in the D32 register*. Replace the lede’s last clause with: “otherwise the pair is not comparable in the sense of D32, so its difference cannot support the memory verdict at issue.” Likewise, `:1023-1028` turns the explicit raise–hold–lower example into a property of every balanced pulse. Say: “The explicit balanced family used in the analytic proof raises, holds, and lowers the energy; a general balanced pulse is only required to return the net energy to its initial value.”

4. **The Lamport object is faithfully compressed but falsely advertised as the full proof** (`:1160-1203`; also the provenance summary at `:2259`). Priority line-by-line comparison against `t1-analytic.md` gives the following result:

   - `⟨1⟩2` (`:1205-1265` versus source `:195-369`) preserves the equations, signs, and charge-phase conclusion. The compression omits (2.4), leaving `X,Y,Z,A` undefined in leaf `⟨2⟩3`; omits the definitions of `B` and `H` in (2.7) while using them in `⟨2⟩7`; and titles `⟨2⟩8` “balanced and unbalanced families” while omitting the unbalanced equation (2.10).
   - `⟨1⟩3` (`:1267-1329` versus source `:371-578`) is mathematically faithful, including the exact `a\ne1` criterion, the leading-moment fence, the butterfly decay, and—most importantly—the `⟨1⟩3.⟨2⟩7` RETRACT. That retraction leaf should display (3.11)–(3.13), because otherwise the reader cannot inspect the “correct algebra / invalid detector” split the leaf discusses.
   - `⟨1⟩4` (`:1331-1400` versus source `:580-776`) preserves (4.3), (4.5)–(4.15), the subtraction order, and the `O(C^{-2})` limitation. Keep explicit that “every compact source” in `⟨2⟩7` is the formal linear identity, while only balanced sources have an operational comparator in this shard.
   - `⟨1⟩5` (`:1402-1440` versus source `:778-861`) preserves every verdict and the physical-argument tag. Leaf `⟨2⟩6` is updated from the shard’s then-“future row” language to the now-registered `JT-B-susc` row; cite `claims/CLAIMS.md:109` there so the provenance of that update is explicit.

   No displayed equation in the 31 leaves changes the source’s value or logical direction. The exact visible label should therefore be “faithful compressed proof map (31 leaves); open the source for the full derivation,” with the omitted definitions restored inline or linked per leaf. Do not call it “in full.”

5. **The centerpiece DC chart does not perform the refit it claims to perform** (`:1466-1475`, `:1530-1535`, JS `:2691-2698`). The code computes a least-squares slope from the three low-frequency points but sets `icept = row.dc.extrapolated_residue` and uses that stored answer as the line’s intercept. The true browser-side fit does numerically reproduce the stored value exactly for the shown row, but the code never computes that intercept. Compute `fitIntercept = (sy - slope*sx)/n`, draw and print `fitIntercept`, then print `fitIntercept - row.dc.extrapolated_residue` as the check (expected `0.000e+00`). Keep the stored value separately labelled “recorded intercept.” This is a logic fix, not a numerical change.

6. **The decisive detector gloss replaces the defined calibration with “dividing the envelope out”** (`:1578-1594`). At finite time the exact nonlinear readout is defined implicitly by `G_pulse(T,T)=G_eq(T-Δu_NL,T-Δu_NL)` and solved by (3.6); at linear order the susceptibility is `(R_g-1)/[2Δκ tanh(κ(T-u_*))]`, equivalently `-δG/∂_T G_eq` under the stated small-response condition. It is not literally obtained by dividing by the plotted exponential envelope. Replace `:1581-1585` with those two definitions, then say only that their *late asymptotics* cancel the same exponential decay.

7. **The recorded-run plots silently use points outside the page’s declared finite-`C` proxy** (`:1603-1647`, especially normalization to `T=40` at `:1627-1641`; embedded window values at `:2421-2424` and `:2447-2450`). The page’s own data set the conservative operational edge at `T_max≈3.16` for `C=10` and `T_max≈10` for `C=100`, while both plots and the fit use `T={5,10,20,40}` and normalize by `T=40`. These samples are legitimate fixed-`C` trajectory/asymptotic diagnostics, but they are not all inside that proxy. Put a visible per-row `T_max` marker or hatch the region beyond it; label the model fits and `T=40` normalization “asymptotic diagnostic outside the conservative finite-`C` proxy”; and distinguish them from the analytic large-`C`-first verdict. Do not leave the reader to infer this from a later table row.

8. **The provenance blanket is false as written** (HTML comment `:22-24` and visible slice note `:620-624`). The page contains an explicitly fitted DC extrapolation and in-browser deterministic transformations of samples (the normalized `Δu` curves), so “No number ... is fitted” and “every number is ... copied” are not true even though the operations are later disclosed. Replace with: “Every displayed value is a sourced closed form, a recorded sample, or an explicitly labelled deterministic transform/extrapolation of recorded samples; no curve crosses an uncomputed scope boundary.” Give the DC chart a visible provenance tag “computed samples + stated least-squares extrapolation,” not merely “Computed sample.”

9. **The pullback overstates what is data-backed, and its primary implementation claim is false** (`:1860-1864`, `:1880-1885`, SVG paths `:1914-1930`). Not “every sentence on this route” is a node, and the specification/definition nodes deliberately have no status. The dependency paths are also hard-coded SVG, so “the solid arrows are not drawn by hand” is untrue even though comments document their source rows. Replace the pullback with: “Each registered result on this sub-map opens to its status, dependencies, proof source, computation, and criticism; definitions and specifications are explicitly statusless.” Then either generate the edge list from an inline snapshot of the `depends-on` fields, or say exactly: “The arrows are a manually rendered snapshot of those fields; the adjacent source comments identify each row.” The former better earns the atlas’s central wow claim.

10. **The atlas count is arithmetically wrong** (`:1880-1902`). The SVG contains eleven clickable objects: one specification, two definitions, seven claim rows, and one separately visualized HOLD scope fence that is *not* a claim row. Replace “Ten nodes” and the `<desc>`’s “Ten nodes” with that exact inventory. This also prevents the HOLD card from looking like an eighth registered claim.

11. **The mock answer turns HOLD into NO** (`:2316-2333`). The question “Does this hold for a pulse that injects energy?” must not open with “No.” The exact answer is: “Not established. The all-profiles generalization beyond the balanced sector is HOLD because no implementable unequal-energy reference protocol has been constructed. The one-kick transfer-matrix algebra exists but carries no detector verdict.”

12. **The other mock answer reimports permanence through the word “stable”** (`:2295-2312`, especially `:2308`). `JT-B-susc` explicitly denies a uniform finite-`C` late-time bound and practical permanence. Replace the “honest answer” with: “Not in the reserved D32 sense. In the declared large-`C`-first window there is a gauge-invariant calibrated susceptibility with exponentially growing readout cost, but no persistent absolute record.”

## 3. Top 10 improvements, ranked by impact/effort

1. **The route withholds its best result until after a 31-leaf proof object.** Immediately after the load-bearing equality at `:1145-1164`, show a compact “what the recorded run does” strip containing the two stop-05 thumbnails—`δG_LR` falls by 16 decades; `Δu_rel` approaches its fitted late value—plus one sentence: “The absolute reading vanishes; the calibrated susceptibility approaches the same DC residue.” Put the full Lamport tree under a closed “Inspect the compressed proof” disclosure, followed by the full-size DC chart. This preserves rigor while moving the first empirical payoff several screens earlier. **Effort: M.**

2. **The ENTHUSIAST/REFEREE control is below the content it controls and referee mode deletes the very exhibits a referee wants** (`:729-770`, `:915-962`, `:1494-1545`, `:1797-1847`, JS `:2530-2549`). Move one global task toggle into the sticky masthead or directly below each stop heading. Split each stop into `.account` and `.evidence-shared`: referee mode should replace only explanatory prose, while keeping the load-bearing equation, charts, witness table, exact claim rows, and Lamport disclosure visible. On stop 05, referee mode should put assumptions/limit/falsifier alongside the two plots, not hide the plots. **Effort: M.**

3. **The DC interaction currently asks the reader to trust the stored intercept while claiming the opposite.** Implement the browser-side intercept calculation specified in FATAL 5, show a three-row micro-ledger under the chart—“browser refit,” “recorded JSON,” “difference”—and use the computed value for the dashed line. Preserve the fourth open point outside the fit; it is excellent visual evidence that the extrapolation rule was preregistered rather than tuned. **Effort: S.**

4. **The two recorded-run panels visually erase the operational-window boundary.** Add per-series vertical markers at `T_max=3.162...` and `9.999...`; use a light hatch to the right labelled “fixed-`C` asymptotic diagnostic; outside conservative proxy.” For the normalized panel, add a two-state control `absolute / divided by Δu_rel(40)`, defaulting to absolute so the expected factor-of-ten `1/C` scaling is visible; keep the normalized overlay as the comparison view and retain the no-dual-axis design. **Effort: M.**

5. **The proof map is rigorous in content but not self-decoding at the exact places compression removed definitions.** Relabel it “31-leaf faithful compression,” restore (2.4) before `⟨1⟩2.⟨2⟩3`, (2.7) before `⟨2⟩7`, (2.10) in `⟨2⟩8`, and (3.11)–(3.13) in the RETRACT leaf; add a source-line link to every level-one heading. Keep the four-level flyover headlines and the ASSUME/PROVE/RETRACT/VERDICT tags unchanged. **Effort: M.**

6. **Stop 01 asks the reader to accept the triangle before displaying any scattering law and then overstates the Fourier hinge.** Replace the generality at `:653-695` with a three-line blackboard on-ramp: a sourced leading soft-photon factor (with its charge, in/out sign, and `q→0` scope); the corresponding Ward-identity role stated as literature context, not a campaign result; and the correctly conditioned retarded final-value identity from FATAL 1. End with: “The question here is narrower: does the soft/DC-to-late-response edge survive when the ruler is a thermal correlator?” **Effort: M.**

7. **Clicking a DAG node throws the reader into a long undifferentiated card stack, so the map ceases to function as a map** (JS `:2575-2594`, cards `:2054-2215`). Render the selected card in a single detail panel immediately below or beside the DAG; keep one card open at a time, preserve focus on the selected node, and provide `Previous / next / back to map`. The existing full cards can supply the panel content unchanged, and permalinks should open the map with that panel selected. **Effort: M.**

8. **The symmetry cluster arrives cold and a mostly green selected sub-map can be mistaken for a solved triangle.** Immediately above the pullback button, ask the missing physics question: “Does a conserved charge or Ward identity enforce the plateau?” Answer: “No registered dependency establishes that edge.” Above the symmetry band, add a two-sentence primer: the tangent is explicitly broken, two candidate charges fail only on a finite grid, pure JT supplies no matter-coupled no-go, and a nonconserved implementer remains possible. Pair this with the corrected eleven-object inventory and a header badge “selected sub-map: 7 PROVED rows; 1 HOLD scope fence; no registered connecting edge.” **Effort: S.**

9. **The decisive explanation uses a glossy but mathematically wrong “divide the envelope out” shortcut.** Replace it with a small two-row definition block: “linear susceptibility: `Δu_rel^lin=-δG_LR/∂_T G_eq` when `|R_g-1|≪1`”; “exact nonlinear readout: solve `G_pulse(T,T)=G_eq(T-Δu_NL,T-Δu_NL)` when `D_g≥2`.” Then show the common late exponential underneath with a brace explaining why the ratio can settle while each absolute expectation difference vanishes. **Effort: S.**

10. **Review-process narration competes with the physics at the moment the page should become less glossy** (`:1479-1491`, `:1650-1686`, plus repeated objection histories in referee panels). Keep the non-circularity result on the main path as one compact statement—“doubling the supplied late value leaves the independently extracted DC intercept unchanged; mutation: 93 pass / 3 fail”—and move the pre-repair numbers, 81/1 history, test coverage list, and objection dispositions into the referee/provenance drawer. The exact 96/96 recorded run should remain, but after the plots and without a large open console dominating the verdict. **Effort: S.**

## 4. Journey check

**Why.** A traditional QED/GR reader understands the intended question immediately, but stop 01 currently offers a literature quotation and an underqualified final-value identity where it needs one concrete soft factor and one carefully scoped bridge. Trust is first damaged at “if a response has a simple pole ... [it] settles” (`:676-689`), because that reader knows the omitted spectral hypotheses. It is damaged again by “two coupled SYK dots” (`:705-710`), which sounds like a different microscopic model. The fix is the narrower blackboard on-ramp in improvement 6 and the two-copy wording in FATAL 2.

**How, detector and comparator.** Stop 02 is the cleanest on-ramp in the route: it names the finite-`N` even CAR algebra, draws the two clocks and the single synchronization event, and says exactly what is scaffolding. Stop 03 then supplies a real scientific turn rather than web ornament: matching conserved charges kills the old unbalanced comparator and leaves the balanced sector. The only needed repair is to say “cannot license D32 memory,” not “measures nothing,” and to distinguish the explicit raise–hold–lower family from arbitrary balanced pulses. At this point the reader is ready to see the result.

**How, soft/DC identity.** The reader gets the right load-bearing equation at `:1145-1157`. Attention is then lost at the exact heading “The proof, at the level you want it” (`:1181-1190`): all four level-one steps are initially closed, but the page still makes the reader cross the entire proof component and its 31 leaves in document order before reaching the numerical witness. The proof is worth keeping; it should be a compact, explicitly compressed inspection branch after a thumbnail of the recorded result. The second trust failure occurs at the DC caption’s claim of an in-page refit (`:1466-1475`) because the JS does not compute the intercept.

**What.** Stop 05 contains the forwardable moment: “absolute difference dies / calibrated shift settles,” immediately followed by the D32/D33 split and the exponential shot cost. The moment is weakened by three choices: normalized curves hide the `1/C` scale, the `T=40` samples are visually unmarked as outside the conservative finite-`C` proxy, and referee mode hides every witness panel. Default to the raw scale, provide the normalized overlay as a toggle, mark the proxy boundary, and keep exhibits visible in both modes. Then the exact claim rows feel like adjudicated results rather than disclaimers.

**Pullback.** The missing dependency edge is the right ending, but the symmetry cluster appears without a question, and clicking it sends the reader far down a stack of cards. Prime the pullback with “Does a Ward identity enforce the plateau?” and the exact answer “no registered edge”; show selected-node detail next to the map. The reader should leave remembering one result and one open edge—not seven green IDs.

## 5. Register check

- **Overcompressed triumphalism:** “three facts that look unrelated turn out to be one fact” (`:653-658`). Rewrite: “In the standard asymptotic formulations of QED and gravity, a leading soft theorem, its Ward identity, and a memory observable are related under explicit boundary and regularity assumptions.”

- **A universal slogan where a scoped definition is needed:** “otherwise ... the comparison measures nothing” (`:987-991`). Rewrite: “otherwise the pair is not comparable under D32 and cannot support the memory claim tested here.”

- **Defensive gloss:** “A balanced pulse is not a trick to make the problem easy” (`:1023-1028`). Rewrite: “The balanced sector is the sector for which this shard constructs a same-charge reference protocol. Its explicit raise–hold–lower family still produces a nonzero leading calibrated susceptibility.”

- **Overclaim at the decisive proof handoff:** “This is the edge of the triangle that survives here, and the proof of it is below in full” (`:1160-1164`). Rewrite: “This establishes the soft/DC-to-late-response edge at leading `O(C^{-1})` in the balanced Schwarzian scope. The 31-leaf object below is a faithful compression; the linked shard is the full proof.”

- **Gloss replacing a definition:** “the calibrated shift is what you get after dividing that envelope out” (`:1578-1585`). Rewrite: “At linear order the calibrated shift is `-δG_LR/∂_T G_eq`; the exact nonlinear readout is the diagonal time shift that reproduces `G_pulse`. Their late asymptotics cancel the same exponential factor, but neither is defined by manually removing a fitted envelope.”

- **Marketing language in a scoped result:** “The famous butterfly shift has zero plateau” (`:1715`). Rewrite: “For the pure Shenker–Stanford shear in the synchronized balanced Schwarzian chart, the calibrated delay has zero plateau and decays as stated below.”

- **Self-evaluation presented as a result:** “the part of this work that is likely to outlive the particular calculation” (`:1739-1744`). Rewrite: “The two definitions make the three statements compatible and expose which conclusion depends on a persistent calibrating ruler.”

- **Repeated moralizing around scope:** “That last sentence is the honest form of the whole route” (`:1787-1793`) and “I will not improvise physics to cover the gap” (`:2360-2364`). Rewrite the first as: “This gives the route’s scoped conclusion: the D33 soft/DC susceptibility survives, D32 memory does not, and no registered symmetry dependency currently connects to either.” Rewrite the second as: “No registered row answers this question. The nearest scoped boundaries are listed below.” The refusal is stronger when it is clinical.

At the decisive moment, reduce the visual register as well as the prose register: equation, raw values, proxy-window markers, residuals, exact status, scope. Mutation history, campaign chronology, and confidence-building adjectives belong in the referee drawer.

## 6. Keep list — do not regress these

- Keep the five-stop authored route and the delayed global pullback. The sequence detector → comparable protocols → soft/DC identity → persistence is coherent and much stronger than opening on the graph.

- Keep the stop-02 apparatus diagram, the single synchronization event, the even-CAR/POVM grounding, and the explicit list of non-observables. This is the route’s best translation from ordinary many-body measurement language into the Schwarzian calculation.

- Keep the stop-03 retraction as a first-class scientific result. Restricting the six unbalanced rows and displaying the missing unequal-energy reference protocol builds trust; do not soften it into a footnote.

- Keep the central equality `(4.9)=(4.13)`, the “nothing fitted” normalization of the analytic soft attachment, the `O(C^{-2})` fence, and the exact finite-amplitude exclusion together. Those four pieces are the claim.

- Keep the four Lamport flyover headlines, all 31 leaves, the ASSUME/PROVE/RETRACT/VERDICT vocabulary, both `[physical argument]` markers, and especially `⟨1⟩3.⟨2⟩7`. The source comparison found no changed equation or reversed inference; repair the compression label and missing definitions rather than rewriting the proof.

- Keep the DC plot’s fourth, visibly open point outside the three-point fit and keep the “agreement too small to draw at this scale” decision. Once the intercept is genuinely recomputed, this will be a very good evidence display.

- Keep the restriction to parameter points 6 and 8, the bit-level embedded values, the explicit deterministic normalization disclosure, separate axes/panels rather than dual axes, and the full witness table. Add window markings; do not add persuasive curves for the six demoted rows.

- Keep the recorded-run wording on the verify affordance and “This page executes nothing.” The real 96/96 result and the red-capable mutation are valuable when subordinated to the physics.

- Keep all three verbatim claims, their PROVED/HOLD scopes, and the D32/D33 cards. The sentence “Satisfying D33 does NOT license the unqualified word ‘memory’” is the conceptual payoff and should remain verbatim.

- Keep the map’s solid dependency edges, dashed context-only edges, dotted scope-fence connector, explicit no-edge divider, and the no-dependency labels on `JT-A-purejt` and `JT-A-notconserved`. This distinction between dependency, context, and scope is exactly what makes the atlas more than a status dashboard.

- Keep the ask component visibly labelled MOCK, its extractive-only answer contract, and its refusal fallback. Change the two inaccurate answer sentences, but preserve the principle that an out-of-scope question returns the held node, missing edge, and finite-`N` gap rather than a synthesized derivation.

- Keep the restrained palette, theme parity, local scrollers for wide content, keyboard-operable DAG nodes, claim permalinks, and the absence of dual axes. For the mobile repair, solve the `overflow-x:clip` fallback and test at 320/360 px without changing the desktop visual identity.
