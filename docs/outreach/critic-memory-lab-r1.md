# Memory Lab audition critic — round 1

## 1. Verdict

Not yet forwardable, but close. The real wow lands when Figure 1 makes the calibrated late response and the vanishing absolute correlator visible together, then Figure 2 lands the independently extracted DC ratio at `1.000109979`/`0.999988369`; the pure-shock zero plateau is an excellent second payoff. The page earns unusual trust through its hatched domain, verbatim data, recorded/live-check separation, and deliberately plain number ledger. That trust presently leaks in a few exact places: the default `C=10` view puts the apparent plateau outside its declared window; approximate energy balance is called exact D32 comparability; unexhibited unbalanced reference protocols are called nonexistent; the free butterfly slider is allowed to inherit a proved operational claim; and the epilogue calls a D33-only result “memory.” Repair those truth fences and shorten the click-heavy runway to the first balanced result, and a traditional physicist could plausibly forward it.

## 2. Truth-constraint audit — FATAL must-fix items

The data spine itself passes audit. The embedded JSON is byte-identical to both the working results file and commit `73b2753` (SHA-256 `9fa222151d75cef67ab007683c467c2ddd762924cf1e5b50a3b4a73e4b2de3a0`). Re-evaluating the browser formulas gives a worst relative discrepancy of `5.147571239258799e-9` over the 80 stored values; omitting the documented column-major transpose worsens that to `0.542475`. The two browser-refitted DC ratios reproduce the stored values to floating-point precision, the `0.011% or better` arithmetic is correct, and the 96/96, 81/1/82, test names, and poison transcripts match their sources. Do not “repair” any of those.

1. **FATAL — the file violates the binding artifact opening convention.** The target starts with `<meta>` and three external-resource tags before `<style>` ([memory-lab.html:1](auditions/memory-lab.html#L1)–[8](auditions/memory-lab.html#L8)); `AUDITION-SPEC.md` requires the file to begin with `<title>` and a `<style>` block. Make `<title>` the first element and conform the resource placement to that convention (prefer dropping the font preconnects/import if necessary); retain no `doctype/html/head/body` wrappers.

2. **FATAL — “exactly solvable gravitational clock system” overstates what supplies the result.** The source response is integrated by fixed-step RK4 and the post-pulse Möbius matrix is least-squares fitted; exact closed forms are then evaluated on that fitted matrix ([memory-lab.html:447](auditions/memory-lab.html#L447)–[450](auditions/memory-lab.html#L450), [765](auditions/memory-lab.html#L765)–[779](auditions/memory-lab.html#L779), [3453](auditions/memory-lab.html#L3453)–[3457](auditions/memory-lab.html#L3457)). Replace the masthead sentence with: “The three notions come apart in a controlled Schwarzian gravitational-clock calculation: a numerically integrated pulse fixes a fitted post-pulse frame, and proved closed forms determine its late detector response.” Reserve “exactly solvable” for the toy.

3. **FATAL — the toy crosses both the mathematics and the vocabulary fence.** The displayed equations give `A=K` and `R=K/x₀` at `γ=0`, so they are numerically equal only after declaring the code's hidden choice `x₀=1` ([memory-lab.html:531](auditions/memory-lab.html#L531)–[533](auditions/memory-lab.html#L533), [2941](auditions/memory-lab.html#L2941), [3013](auditions/memory-lab.html#L3013)–[3015](auditions/memory-lab.html#L3015)). More importantly, “the ratio … still remembers” ([memory-lab.html:603](auditions/memory-lab.html#L603)–[610](auditions/memory-lab.html#L610)) grants unqualified memory to a toy ratio that has no DC datum and therefore does not establish D33, while its displayed shot count is for `A(t)`, not a derived ratio estimator. Declare `x₀=1` visibly, or say only that both registers remain nonzero; replace the conclusion with: “The calibrated ratio remains nonzero. This toy has no soft-theorem/DC datum, so it establishes neither D33 nor any Schwarzian conclusion.” If the ratio's cost is retained, state and derive its i.i.d. additive-noise estimator rather than borrowing the `A(t)` readout.

4. **FATAL — D32 is advertised as quoted in full but is truncated.** The page's claim at [memory-lab.html:685](auditions/memory-lab.html#L685) and quote ending at [703](auditions/memory-lab.html#L703) omit the final source parenthesis. Append verbatim: “(Relation to D26/D27: those are charge-ledger/counting definitions; D32 is the detector register. The reconstruction obligation connecting them is tracked, not assumed.)” Alternatively, stop saying “quoted in full,” but the builder brief and spec prefer the full quote.

5. **FATAL — the displayed detector normalization is incomplete for number provenance.** The general detector formula at [memory-lab.html:768](auditions/memory-lab.html#L768)–[769](auditions/memory-lab.html#L769) omits `𝒩_Δ`; the next equation restores it, while the JS/results convention evaluates `𝒩_Δ=1`. Since “exact formula” promises that the displayed formula is the evaluated formula, add “The numerical convention below is `𝒩_Δ=1`” beside the first equation, or include the factor there and bind it visibly to one.

6. **FATAL — a small numerical energy residual is promoted to exact D32 comparability.** “To that accuracy … agree on the exactly conserved charges, so they are comparable in the sense of D32” ([memory-lab.html:3197](auditions/memory-lab.html#L3197)–[3206](auditions/memory-lab.html#L3206)) and “the two protocols are comparable” in the ledger ([memory-lab.html:3450](auditions/memory-lab.html#L3450)–[3452](auditions/memory-lab.html#L3452)) are stronger than the adjudicated claim. D32 requires agreement on every exactly conserved allowed charge; the numerical rows have residual energy changes `2.8260756e-8` and `2.8271607e-9`, and the registered result is only leading one-soft/tree order `O(C^{-1})`. Required label: “energy-balanced at leading order; the displayed nonzero energy change is the finite numerical/nonlinear residual. The operational conclusion is restricted to the registered leading-order balanced scope and is not an inference of exact finite-`C` D32 comparability from this residual.”

7. **FATAL — “not exhibited” is silently strengthened to “does not exist.”** The unbalanced banner says “there is no admissible reference run” ([memory-lab.html:3211](auditions/memory-lab.html#L3211)–[3214](auditions/memory-lab.html#L3214)), and the ledger says “no admissible reference protocol exists” ([memory-lab.html:3450](auditions/memory-lab.html#L3450)–[3452](auditions/memory-lab.html#L3452)). The ground truth is only that no implementable two-dot reference protocol with the same measured unequal one-sided energies **has been exhibited**. Use that exact weaker statement in both places; the all-profiles generalization remains HOLD, not refuted.

8. **FATAL — Figure 2's fixed scope sentence becomes false in a reachable UI state.** Selecting any unbalanced cell redraws Figure 2 from that row ([memory-lab.html:3370](auditions/memory-lab.html#L3370)–[3436](auditions/memory-lab.html#L3436)), while the visible copy still says “the figure describes the two balanced rows only” ([memory-lab.html:862](auditions/memory-lab.html#L862)–[873](auditions/memory-lab.html#L873)). Best fix: suppress Figure 2, its DC/vertex readout, and any “numbers the verdict rests on” language for unbalanced selections, replacing them with the O9 diagnostic-only fence. Acceptable fix: make the heading, tag, caption, and 0.011% paragraph state-dependent and label an unbalanced rendering “formal Schwarzian kernel diagnostic; licenses no detector or memory conclusion.”

9. **FATAL — a scaling proxy is presented as a literal shot count.** The readout and decisive ledger say “Shots for 10% error” ([memory-lab.html:3361](auditions/memory-lab.html#L3361)–[3365](auditions/memory-lab.html#L3365), [3476](auditions/memory-lab.html#L3476)–[3480](auditions/memory-lab.html#L3480)). The source states only `M_shots = O(e^{4ΔκT}/η²)` up to unknown protocol-dependent variance and finite-difference calibration constants. Rename both rows “shot-cost scaling proxy at `T_max`,” retain the computed model value, and append exactly that constants caveat. It supports “practical permanence is not established”; it is not a calibrated experimental shot count.

10. **FATAL — the butterfly instruction becomes mathematically false when its own `a` slider moves.** “Moving `α` … leaves that late value at exactly zero” ([memory-lab.html:929](auditions/memory-lab.html#L929)–[931](auditions/memory-lab.html#L931)) is true only at `a=1`; for `a≠1` the late value is `−log a/κ`. Make the sentence dynamic, or replace it with: “At `a=1`, varying `α` changes only the transient and the late value remains zero. For `a≠1`, `α` still drops out, but the late value is `−log a/κ`.”

11. **FATAL — the free formal-frame instrument inherits an operational claim it does not establish.** Figure 3 lets `(a,α)` roam through frames no computed, implementable, energy-balanced pulse is shown to realize; the caveat arrives only after the controls and chart ([memory-lab.html:914](auditions/memory-lab.html#L914)–[947](auditions/memory-lab.html#L947)). Yet the live readout declares, “That is claim JT-noplat-abs” ([memory-lab.html:3670](auditions/memory-lab.html#L3670)–[3675](auditions/memory-lab.html#L3675)). Put a persistent pre-control tag “EXACT FORMULA · FORMAL FRAME FAMILY · REACHABILITY NOT COMPUTED,” rename “Add a dilation” to “Explore a formal `a≠1` frame,” and replace the claim sentence with: “This is the algebraic decay mechanism used by JT-noplat-abs; that claim additionally assumes a compact implementable energy-balanced pulse and the registered large-`C`-first scope.” Keep the pure `a=1` Shenker–Stanford statement separately tied to JT-bfly-decay.

12. **FATAL — the epilogue grants unqualified memory where the adjudication says D32 fails.** The prose says the page has stood “in the memory corner,” the SVG/ARIA label that corner “memory effect,” and the diagram puts D32 and D33 under the same filled node ([memory-lab.html:1038](auditions/memory-lab.html#L1038)–[1048](auditions/memory-lab.html#L1048), [3805](auditions/memory-lab.html#L3805)–[3814](auditions/memory-lab.html#L3814)). D33 explicitly does not license unqualified memory, and this system is the adjudicated D33-pass/D32-fail instance. Rewrite the prose as: “This page tested the late-response corner and found a calibrated D33 susceptibility, not D32 memory.” Rename the node “late response” with sublabel “D33 passes · D32 fails,” and call the highlighted edge “soft datum ↔ calibrated susceptibility at leading order.”

## 3. Top 10 improvements, ranked by impact/effort

1. **The default `C=10` view asks the reader to infer the plateau almost entirely from the hatched, beyond-window region.** Default `selected` to the balanced `C=100` row (index 7), where `T_max≈10` reaches the first plateau-quality sample; add a compact “compare finite-`C` proxy” button that toggles the paired `C=10` row and explicitly says only `T=2` is inside its proxy window. This makes the first real figure demonstrate the declared limit order instead of asking the caption to rescue it. **Effort: S.**

2. **The result arrives too late for the forwardability test.** Immediately after the Schwarzian one-paragraph on-ramp, add an unglossy three-line result strip: “Balanced Schwarzian rows, leading `O(C⁻¹)`, `C→∞` first: calibrated susceptibility = independently extracted DC datum to `0.011%` or better; the allowed absolute correlator has no nonzero late plateau; the pure butterfly shift has zero plateau. No uniform finite-`C` bound or practical permanence is claimed.” Each line should link-scroll to its figure/ledger row. **Effort: S.**

3. **A parameter selection currently changes the data without changing all downstream claim language.** Treat “balanced operational” and “unbalanced formal diagnostic” as two station modes: in `renderStation()`, toggle a mode class and update/hide Figure 2, the DC/vertex readout, shot-cost conclusion, plate intro, and claim-bearing captions together. In diagnostic mode, keep the raw curves and ledger only behind a “show formal diagnostics” disclosure headed with the verbatim O9 fence. **Effort: M.**

4. **The butterfly sliders visually imply pulse reachability before the caveat is read.** Put the formal-frame provenance tag and reachability sentence above the sliders, make the `a=1` pure-shock preset the only claim-bearing state, and label every `a≠1` state “algebraic comparison—not a realized pulse in this page's dataset.” Keep the exact family because it teaches why `α` drops out and `a` survives. **Effort: S.**

5. **The six green browser checks look more homogeneous and more independent than they are.** Add a “check type” column: checks 1, 4, 5, and 6 are browser recomputations; checks 2 and 3 are threshold comparisons against stored diagnostic scalars. Under check 4 say: “validates serialization, matrix orientation, and rendering formulas; it does not rerun the sourced ODE.” Tag the panel with both `Exact formula` and `Computed sample`. **Effort: S.**

6. **Figure 1's continuous line has hybrid provenance that the two existing tags do not quite express.** Change its legend/tag to “PROVED CLOSED FORM · FITTED COMPUTED FRAME,” and put the fit residual beside the legend, not only in the later ledger. Keep “not an interpolation of markers”; add “the fitted matrix is the input, so this line is not an independent second trajectory computation.” **Effort: S.**

7. **Four reveal clicks plus two full definitions create a seminar-style click wall between the toy and the result.** Show all five ingredients as a compact numbered stack by default, with the first highlighted; let one “walk me through” control progressively emphasize them rather than withhold them. Keep D32/D33 collapsed behind “read the exact campaign definitions,” while showing a two-row operational comparison (`fixed observable/nonzero late difference` versus `gauge-invariant calibrated functional/DC match/cost`) in the main path. **Effort: M.**

8. **The spectacular `10¹⁸–10²¹` cost scale is present but conceptually detached from the plateau it qualifies.** On Figure 1, place a small annotation at `T_max` reading “cost proxy at this time: `7.27×10²¹` for the default row; unknown multiplicative calibration constants,” and repeat “practical permanence not established” immediately below the calibrated plateau value. This is the honest sting in the result, not boilerplate. **Effort: S.**

9. **Gaussian rows hide a sign change by plotting only `|δG|` and relegating the warning to a subtitle.** For a sign-changing selection, draw positive and negative markers with distinct open/filled shapes, mark the zero crossing in the upper margin, and change the readout label to `signed δG_LR(T=40)`; retain the log-magnitude panel but never make the absolute-value transformation the only visible encoding. **Effort: M.**

10. **The load-bearing displayed equations degrade to raw TeX when the MathJax CDN is unavailable.** Hand-set the three indispensable equations (toy registers, Figure 1 detector/readout, Figure 3 late limit) in semantic HTML/SVG as a fallback, then let MathJax enhance the full derivations when available; also remove the Google-font dependency or accept the declared fallback stacks without preconnects. The artifact should remain scientifically readable from `file://` with networking disabled. **Effort: M.**

## 4. Journey check

**Why.** The opening question and explicitly schematic test-mass ring are an excellent on-ramp: a GR-trained reader knows what is being asked and is told exactly what has not been computed. The toy then earns the absolute/calibrated split from a displayed solvable equation. The first trust nick occurs in its conclusion, where “still remembers” outruns the definition the page is about to impose and where the hidden `x₀=1` is needed for “same number.” Make the toy end with the observable fact only—one fixed difference decays, one normalized ratio stays nonzero, and its readout needs a noise model—then let D32/D33 adjudicate the names.

**How.** The five ingredients are the right ones and the order-of-limits step is unusually good. Attention is most likely lost at the fourth manual reveal, just before the reader is rewarded with two long repository definitions ([memory-lab.html:633](auditions/memory-lab.html#L633)–[741](auditions/memory-lab.html#L741)). A traditional physicist will understand the list faster than the interface permits. Show the compact five-step structure at once, offer the verbatim definitions as an evidence layer, and preserve the limit-order quotation in the main path.

**What.** The reader's intended discovery is excellent: select a real balanced run, see `Δu_rel` flatten while `δG_LR` falls, and watch the independent DC intercept meet it. Trust is damaged at the exact first rendering because the default `C=10` row has `T_max=3.162`, leaving only the `T=2` stored marker inside the declared proxy window while the apparent plateau sits in hatching; the caption then asks the reader to choose `C=100` ([memory-lab.html:823](auditions/memory-lab.html#L823)–[834](auditions/memory-lab.html#L834)). Default to balanced `C=100`, surface its leading-order/finite-`C` fence next to the title, and make `C=10` the comparison. A second trust break occurs if curiosity leads to an unbalanced cell: Figure 2 changes to unbalanced data while its scope sentence remains balanced-only. The station-mode fix in item 3 must make scope follow state atomically. Once those are fixed, the decisive plate and pure-shock endpoint complete the arc cleanly; the epilogue should then say the page reached D33 but not D32, rather than retroactively renaming the result memory.

## 5. Register check

The overall voice is serious and collegial. These are the places where it drifts into gloss, over-certainty, or the wrong campaign noun:

| Location | Offending copy | Replacement |
|---|---|---|
| Masthead, [447–450](auditions/memory-lab.html#L447) | “come apart on an exactly solvable gravitational clock system” | “come apart in a controlled Schwarzian gravitational-clock calculation, combining a numerically integrated pulse with exact post-pulse detector formulas” |
| How step 2, [652–656](auditions/memory-lab.html#L652) | “their difference is not a record of anything” | “their difference is not an admissible D32/D33 memory comparison; it may still be reported as a formal diagnostic” |
| Toy conclusion, [607–610](auditions/memory-lab.html#L607) | “The pulse was not forgotten … [the ratio] still remembers.” | “The fixed difference decays with the ruler, while the calibrated ratio remains nonzero. No memory label follows from this toy.” |
| Toy live verdict, [3013–3015](auditions/memory-lab.html#L3013) | “There is nothing to argue about here: the apparatus remembers” | “For the declared `x₀=1`, both readouts remain nonzero at constant cost. This is the persistent-ruler case the next definition will classify.” |
| Figure 3 button/readout, [926–931](auditions/memory-lab.html#L926), [3676–3678](auditions/memory-lab.html#L3676) | “Add a dilation”; “the only entry that matters” | “Explore a formal `a≠1` frame”; “entry controlling the late limit” |
| Decisive butterfly readout, [3659–3667](auditions/memory-lab.html#L3659) | “a time delay, and it is gone” | “For `a=1`, `lim Δu_rel^NL=0`; at `T=30`, the residual is the displayed `O(e^{-κT})` transient, within the stated `O(e^{-2κT})` correction.” |
| Epilogue, [1038–1043](auditions/memory-lab.html#L1038) | “standing in the memory corner” | “testing the late-response corner: D33 is satisfied at leading order in the balanced scope, while D32 fails” |

At the decisive moment, prefer the ledger's register everywhere: observable, protocol, limit order, residual, and scope. “A thermal reference frame erodes its own ruler” is vivid but source-faithful and may stay because it follows—not replaces—the numbers.

## 6. Keep list — do not regress these

- **Keep the opening question and the test-mass ring's `SCHEMATIC` treatment.** The assumed burst and assumed offset are named in the panel itself; this establishes the question without laundering a drawing into evidence.

- **Keep the toy's actual equation, closed-form solution, and hard transfer fence.** The JS evaluates the displayed `K`, `A(t)`, and `R` formulas correctly. Repair only the hidden `x₀=1`, noise/cost assumptions, and memory wording; do not replace the toy with a qualitative animation.

- **Keep the five-way provenance key and the byte-for-byte embedded results file.** The source/commit comment is unusually good, and the embedded block really is identical to commit `73b2753`. The distinction among exact formula, computed sample, not computed, schematic, and recorded run is the page's trust architecture.

- **Keep the 9×6 parameter map with 8 filled and 46 hatched cells.** Refusing to interpolate is exactly right. Add only a sentence that the displayed frame is an editorial inspection grid, not a theorem-defined parameter domain.

- **Keep Figure 1's paired linear/log panels, stored markers, independent DC line, and finite-`C` hatching.** The viewer can see the two registers separate, and the caption correctly calls the cutoff a scaling convention rather than a uniform error bound. Change the default row and hybrid provenance wording, not the basic figure.

- **Keep Figure 2's non-circular construction.** The ringed `ω=0.2` point, the browser-refitted lowest-three intercept, the three-horizon movement, the measured-charge normalization, and the decoy regression together make the `0.011%` agreement scientifically interesting rather than ornamental.

- **Keep the unglossy 16-row decisive plate.** It is the strongest register choice on the page. Correct the comparability, protocol-existence, and shot-proxy labels, but do not turn it into cards, badges, or a celebratory animation.

- **Keep the recorded-run/browser-arithmetic split.** The explicit “this page does not execute Julia,” real test-set names, pre-repair RED, nonzero poison exits, and fail-closed no-JSON contract are excellent. Refine the browser check taxonomy; do not remove the adversarial history or add a fake rerun button.

- **Keep the careful numerical implementation.** The column-major Möbius transpose is necessary and correctly handled; `expm1`/`log1p` avoid late-time cancellation; the closed-form reconstruction residual is genuinely `5.15e-9`; and the stable butterfly formula is materially better than naïve `T−arcosh(D/2)`. These are worth a short visible provenance note, not a rewrite.

- **Keep the exact claim rows and the open-problems paragraph.** The three claims match `claims/CLAIMS.md`, the `0.011%` scope is correctly tied to r2a, and the final paragraph correctly retains the leading-order, nonuniform-window, finite-`N`, unbalanced-HOLD, and finite-amplitude exclusions. Repair the triangle label around them; do not dilute or paraphrase the claims themselves.
