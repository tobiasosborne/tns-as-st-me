# Butterfly Trial audition critic — round 1

## 1. Verdict

Yes, after a focused truth-and-compression repair: a traditional physicist could forward this because the real wow is unusually good—the famous horizon translation is separated from an operational record, the absolute correlator is shown to decay, and the surviving DC/susceptibility identity has an actual adversarial history rather than a victory-lap narrative. The wow lands first at the exact \(a=1\) butterfly law and lands again at the decoy test; it leaks because the page makes the reader traverse a long process story before delivering the three-sentence result, lets four-point fits look more decisive than the theorem, and contains several trust-damaging scope/provenance errors at precisely the points where the interface claims unusual rigor. Fix the items below and preserve the restrained, evidence-led visual language: the concept is strong enough to forward.

## 2. Truth-constraint audit — FATAL, all must be fixed

All bare line references in this section are to butterfly-trial.html.

1. **The Wigner phase is on the wrong branch, and the accompanying decay claim is not sourced.** At butterfly-trial.html:631–655, the principal-value expression \(\arctan[(\Gamma/2)/(E_r-E)]\) jumps at \(E=E_r\), so its displayed derivative is not the smooth Lorentzian claimed there. Restrict the toy explicitly to a zero-background Breit–Wigner resonance and use a continuous branch, for example
   \[
   \delta(E)=\frac{\pi}{2}+\arctan\!\frac{E-E_r}{\Gamma/2},\qquad
   2\hbar\,\delta'(E)=\frac{\hbar\Gamma}{(E-E_r)^2+(\Gamma/2)^2}.
   \]
   Then add a precise source. The pointwise statement that every bounded-region observable tends to zero is stronger than the displayed resonance formula and is not automatic from “short range” plus “no bound-state component”; either cite a local-decay theorem with its hypotheses or replace it by an explicitly solved outgoing packet/projector calculation. Until then Exhibit A-1 is neither an exact sourced toy nor mathematically correct at resonance.

2. **The JT on-ramp replaces the two-boundary system by one mode and incorrectly says the copies are coupled.** At :773–783, “coupled” and “a single soft mode: a reparametrization \(t(u)\)” distort the setup actually used in t1-analytic.md (0.2)–(0.4). Replace the paragraph with: “Take two copies prepared in the thermofield-double state. In the Schwarzian regime each boundary has a trajectory \(t_L(u)\) or \(t_R(u)\) with stiffness \(C\). A simultaneous \(SL(2,\mathbb R)\) Möbius transformation is redundant; the allowed two-sided correlator depends on their relative configuration. SYK supplies a microscopic realization of this effective regime, but no finite-\(N\) conclusion is used here.” If a dynamical inter-copy coupling is intended, it must be exhibited; none is part of the stated protocol.

3. **The displayed D33 definition omits a binding normalization clause, and its drawer is falsely labelled “both definitions, verbatim.”** At :733–741, add “with normalization fixed by measured injected charge” before “nothing fitted”; this is clause (iii) of D33, not optional detail. At :743–755, the drawer contains only D33’s closing paragraph, not either full definition. Either include D32 and D33 verbatim or rename the drawer “D33 closing scope paragraph, verbatim.” Do not leave a provenance label that its content contradicts.

4. **The balance gate says the measured charge decides, but the JavaScript decides from the profile name.** The prose and table at :917–924 and :930–966 present exact/measurement-based sector classification; buildLedger() and describeLedger() at :2448–2483 instead set bal from profile === "derivative_gaussian_balanced", even though the displayed full-run energy changes are nonzero. The adjudicated statement is narrower: those two profiles are energy-balanced at the retained leading order, with full-run residuals \(2.83\times10^{-8}\) and \(2.83\times10^{-9}\). Do not invent a numerical tolerance. Embed a source-owned scope field such as balance_scope: "leading-order balanced", display “balanced at leading order; full-run residual …,” change “Rows … are exact statements” accordingly, and reserve “same-energy reference exists” for the stated leading Schwarzian scope. If the interaction is meant to classify from \(\Delta E_R\), implement and display a justified criterion instead of testing the profile string.

5. **Four computed points are allowed to prove an asymptote on screen.** At :990–995, “Two models … and no room to argue,” the vote at :1058–1063, and the settlement at :2560–2577 let the four-point AIC choice license “no late plateau at all.” The correct statement is: “On the four-point fit window, the exponential model is overwhelmingly preferred; independently, claim JT-noplat-abs proves the late decay in the stated balanced, nonsingular, large-\(C\)-first scope.” Put the theorem beside the fit before the vote and make the answer option say “the computed window selects decay; the theorem then fixes the asymptote.” Also distinguish points outside the declared finite-\(C\) proxy: for \(C=10\), every fitted point is beyond \(\kappa T_{\max}\simeq3.16\). The fit is a diagnostic there, not a finite-\(C\) validity certificate.

6. **Exhibit B′ silently interpolates computed samples.** drawDU() at :2243–2245 draws an unlabeled polyline through the five computed values, contradicting the page-level promise at :596 that scope boundaries are never silently interpolated. Remove that connector. The two explicitly labelled fitted models may remain; otherwise call any connector “visual guide only” and stop it at declared boundaries. Add the same finite-\(C\) proxy marker/hatching used in Exhibit B so the plateau graphic cannot visually erase its own scope.

7. **D33 is called “memory” unqualified at the decisive moment.** At :1194–1197, “a genuine soft-theorem / memory identity” violates the binding sentence quoted by the page itself: satisfying D33 does not license unqualified memory. Replace it with “a genuine zero-frequency / calibrated-susceptibility identity” or “a soft-residue / D33 identity.”

8. **The second verdict card proves more observables fail than the claim row proves.** At :1408–1412, JT-noplat-abs establishes the displayed asymptotic form for the same-energy absolute correlator difference \(\delta G_{LR}\); “So no fixed allowed observable of this family carries a record” reads as a no-go theorem over a family of allowed observables. Replace it with “So this fixed allowed observable, \(G_{LR}\), carries no nonzero D32 plateau in that scope,” and change the badge to “No D32 plateau in \(\delta G_{LR}\)” if needed. Do not turn one observable theorem into an existential no-memory theorem.

9. **The provenance taxonomy makes claims the page does not satisfy.** The masthead at :592–596 defines every “Exact formula” as one evaluated by this page, but the readout definition (:793–826), shock matrix (:829–859), butterfly law (:1154–1183), and historical circular-tail equation (:1211–1243) are not evaluated by the JavaScript. Split the category into “PROVED/SOURCED FORMULA” and “PAGE-EVALUATED FORMULA,” or change each badge. The footer at :1800–1805 also says everything comes from one repository, nothing was regenerated, and the results JSON is embedded verbatim. Exhibit A-1 is expressly non-repository; the page recomputes derived statistics; and only selected JSON fields are embedded in a new wrapper (the selected numeric values do match the source value-for-value). Required wording: “Campaign quotations and sample inputs come from the listed repository files. Selected fields from schwarzian_memory.json are copied value-for-value; derived statistics are recomputed in-browser. Exhibit A-1 is an externally sourced textbook model.” Add that external source.

10. **Decision 2 can display numbers for the wrong selected run.** settle("2", …) fills RSS/AIC values once at :2633–2637; changing the shared run selector at :2676–2677 redraws the exhibits but does not refresh an already-open settlement. A reader can therefore select \(C=100\) while the ruling still displays \(C=10\) statistics. On every row change, refresh Decision 2 if it is open (or bind those values to the same render function as Exhibit B), and include the run label in the settlement so provenance remains visible.

## 3. Top 10 improvements, ranked by impact / effort

1. **State the result before asking for twenty minutes.** Add a three-line “finding at a glance” immediately below the deck: “In the Schwarzian EFT, the pure butterfly translation has zero late plateau. The fixed two-sided correlator also decays. A different object—a balanced, calibrated large-\(C\)-first susceptibility—survives and equals a DC soft residue.” Keep the votes as the route by which the reader checks those sentences, not as a suspense device hiding them. **Effort: S.**

2. **Move the exact butterfly law ahead of the model-selection chart.** In Cross-examination II, place “The butterfly law” and equation (3.15) directly after the act head, with the full scope sentence visible. Follow it with Exhibit B as a numerical check of the broader JT-noplat-abs law. The decisive screen should therefore read theorem → scope → computed diagnostic, with no courtroom ornament inside that block. **Effort: M.**

3. **Make the fit visually honest about both evidence strength and validity window.** Replace “no room to argue” with “strong preference on this four-point window”; hatch/dim the region to the right of the conservative \(\kappa T_{\max}\) marker; distinguish “computed outside finite-\(C\) proxy” from “outside fit window”; remove the B′ data connector; and put the exact JT-noplat-abs asymptotic directly under the chart. Keep the RSS ratio and \(\Delta\)AIC—they are excellent diagnostics once they are not carrying the theorem. **Effort: M.**

4. **Make the genuine independence in Exhibit C visible instead of narrating it.** Above the chart, add a compact three-row ledger: “late detector: bilocal at \(T=40\) → \(\Delta u_{\rm rel}\)”; “DC route: fitted Möbius tail + Abel transform + \(\omega^2\) intercept → residue”; “soft route: linearized Schwarzian source + invariant bilocal vertex, normalized by measured injected charge → vertex prediction.” Then show the two ratios in large type: intercept/late and intercept/vertex. Display equation (4.13) and say explicitly that the fitted-matrix DC and late detector share trajectory data, whereas the linear-vertex prediction is the independent soft-side check. This is the second wow and currently the chart cannot show it. **Effort: M.**

5. **Let the reader construct the comparator rather than merely inspect a ledger.** Replace or augment the static three-row charge table with two controls: left reference exponent \(\kappa_L\in\{\kappa_i,\kappa_f\}\) and right reference exponent \(\kappa_R\in\{\kappa_i,\kappa_f\}\). Recompute \(E_L=C\kappa_L^2/2\) and \(E_R=C\kappa_R^2/2\) beside the pulsed pair; only the matched pair unlocks “comparable.” For the unbalanced case, end with “algebraically matchable, but no implementable two-dot preparation is supplied”—the actual obstruction. This turns the best conceptual point into physics the reader performs. **Effort: M.**

6. **Replace the incorrect one-mode setup with one economical two-boundary diagram.** Label \(t_L(u)\), \(t_R(u)\), common \(SL(2,\mathbb R)\) redundancy, right-boundary compact source, and the allowed cross-boundary observable \(G_{LR}\) in one screen. Copy: “Two TFD copies; no inter-copy interaction is assumed during this protocol. Simultaneous Möbius motion is gauge; the relative bilocal is observable.” This will save more explanation than it adds. **Effort: S.**

7. **Cut repeated review transcript from the main path.** Keep one terse critic quotation per cross-examination and move the long Objection 1/4/6 reproductions, five-objection digest, mutation transcript, commit chronology, and source inventory behind a single “Full adversarial record” appendix after the verdict. The main path should be Exhibit A → operational definitions → setup → three decisions → verdict, comfortably under 15 minutes; the case history remains available for the colleague who wants it. **Effort: M.**

8. **Use provenance labels that answer one question each.** Adopt four badges: “PROVED FORMULA,” “PAGE-EVALUATED FORMULA,” “COMPUTED SAMPLE,” and “RECORDED RUN/QUOTE.” Add a small local line to derived tables: “derived here from sample X using formula Y.” Change “embedded verbatim” to “selected fields copied value-for-value.” This is a small edit with a large trust return because provenance is the page’s signature promise. **Effort: S.**

9. **Make each visit an unprejudiced trial and keep linked state coherent.** Do not auto-reveal prior votes from localStorage; either store them only for the current tab or offer a masthead “Reset decisions” control and default to a fresh record. When run \(C\) changes, update every open readout and ruling that depends on it, especially Decision 2. Preserve the theme preference separately. **Effort: S.**

10. **Give direct-file readers a legible mathematics fallback.** The page is otherwise self-contained, but a failed MathJax CDN leaves raw TeX at the decisive screens. For the seven load-bearing equations, include readable hand-set HTML/SVG fallback that MathJax replaces on successful load, or inline the already-rendered SVG forms. A small “Math rendering unavailable; formulas remain available as text” failure notice is the minimum acceptable fallback. **Effort: M.**

## 4. Journey check

**Why.** The masthead poses a real question in familiar language, and the Wigner-delay versus displacement-memory contrast is the right bridge from scattering/GR to the operational distinction. Trust is first damaged at the exact moment the intended audience checks the algebra—Exhibit A-1’s phase at :638—because the principal arctangent jumps while the next equality differentiates a continuous branch. Repair that equation, source the local-decay statement, and replace the universal “Physics has been using…” claim with a page-local statement about the two registers.

**How.** D32/D33 correctly identify the test, but the reader then meets an inaccurate “single soft mode” setup and a long definitions/transcript interval before doing physics. Cross-examination I is the strongest part of the journey: writing \((E_L,E_R)\) makes the retraction inevitable. Make that comparator constructive and qualify leading-order balance. Attention is then lost at :990–1074: the page says four points leave “no room to argue,” asks for an asymptotic verdict, shows another near-flat fit, and still has not displayed the exact \(a=1\) law. A skeptical physicist will either object to the inference or start skimming. Move the law and JT-noplat-abs formula before the fits, mark the finite-\(C\) proxy honestly, and let the plots corroborate rather than adjudicate.

**What.** Cross-examination III regains attention because the circular transform and decoy mutation are concrete and falsifiable. Its surviving result is harder to see than its review history, however: the genuinely interesting equality is buried in a digit table after a chart whose two lines visually coincide. Put the three computational routes and equation (4.13) on that screen, then deliver the existing three-card verdict immediately. The verdict itself is excellent in architecture, but it arrives after roughly half the file’s reader-facing content. Everything after it—history, full objections, dockets, sources—should read as optional evidence, not a continuation required to discover the answer.

## 5. Register check

- **Pop-science universal:** “Physics has been using one word, memory, for two different jobs.” Rewrite: “For this case we need two operational registers that ordinary language can blur: a nonzero late difference in a fixed observable (D32), and a calibrated ratio with a nonzero late limit tied to a DC datum (D33).”

- **Loose at the butterfly definition:** “\(\alpha\) grows exponentially with how early the perturbation was thrown in. That exponential growth is the butterfly effect.” Rewrite: “In the Shenker–Stanford double-scaling regime, \(\alpha=(E/4M)e^{Rt_w/\ell^2}\); at fixed small \(E/M\), earlier insertion produces exponential boost sensitivity. The late detector question is separate from that growth.”

- **Arrogant numerical inference:** “Two models, the same four computed points, and no room to argue.” Rewrite: “Two candidate models are compared on the same four-point window. The fit strongly selects the exponential there; the asymptotic conclusion comes from the theorem below.”

- **Courtroom flourish at the exact result:** “no plateau at all—not a small one, none.” Rewrite: “For the pure translation \(a=1\), the constant term vanishes exactly and the leading detector response is \(-\alpha\kappa^{-1}e^{-\kappa(T-u_*)}\).”

- **Forbidden D33 shorthand:** “That is a genuine soft-theorem / memory identity.” Rewrite: “That is a leading-order identity between the calibrated D33 susceptibility and the zero-frequency soft residue.”

- **Gloss where mechanism should appear:** “the ruler is made of the same stuff as the signal.” Rewrite: “Both \(\delta G_{LR}\) and the equilibrium calibration \(\partial_TG_{LR}^{\rm eq}\) decay as \(e^{-2\Delta\kappa(T-u_*)}\); their ratio can tend to a constant while neither absolute signal does.”

- **Process self-congratulation:** “attacked twice by a critic who recomputed rather than reviewed.” Rewrite: “Within the declared Schwarzian-EFT scope, the analytic formulas were independently recomputed and the numerical implementation passed a regression that fails when the circular tail is restored.” This says what happened without advertising the process as a credential.

At the decisive screens, remove reveal animation, courtroom lead-ins, and voice rails from the theorem/scope/value blocks. Keep those devices around the decisions; let the result itself become visibly flatter and less glossy, as the spec requires.

## 6. Keep list — do not regress these

- **The central editorial choice.** Making the negative butterfly result the plot rather than an apology is exactly right. Preserve the distinction “transient relational time delay” versus “fixed-observable record.”

- **The preliminary vote’s anti-gimmick discipline.** “Settles nothing, on purpose” is an excellent contract with the reader. There is no score, badge, avatar, or fake branching.

- **The two-register architecture.** D32 and D33 are the conceptual payload. Preserve their side-by-side presentation, the comparability precondition, the measurement-cost requirement, and the explicit sentence that D33 does not license unqualified memory; repair only the missing normalization and false drawer label.

- **The charge-sector retraction.** The \((E_L,E_R)\) ledger, full Objection 1, prover retraction, and “a correct computation of an inadmissible comparison” are unusually clear. Preserve the physics and the candour; only make the leading-order status and interaction logic exact.

- **The in-page numerical recomputation.** The embedded values for rows 5 and 7 match the source JSON value-for-value. The JavaScript genuinely recomputes the constant/exponential RSS and AIC convention and the three-point \(\omega^2\) intercept; the resulting \(C=10\) RSS ratio \(6.83\times10^{11}\), \(\Delta{\rm AIC}\approx107.0\), and both horizon families reproduce the stored values. Keep the recomputation, the stored-versus-derived columns, and the residual strip.

- **The fit controls and caveat.** Switching run/model and exposing residuals makes the model comparison inspectable. The B′ caption’s sentence “Four points are four points, and the theorem, not the model selection, is what carries the claim” is exactly the right sentence; move its discipline earlier rather than deleting it.

- **The circularity demonstration.** The historical \(1/(1+\omega^2)\) decoy, the target-valued-tail diagnosis, the recorded 96/96 green run, and the 93-pass/3-fail reverted-tail run form the best implementation argument on the page. Preserve the explicit “This page executes nothing” label.

- **The exact butterfly law.** Equation (3.15), its \(a=1\) explanation, and the Shenker–Stanford geodesic cross-check are the primary wow. Do not simplify away the energy-balanced, synchronized, Schwarzian-future-chart, large-\(C\)-first scope.

- **The verdict-card separation.** Three cards—JT-bfly-decay, JT-noplat-abs, JT-B-susc—are the correct ending. Preserve the claim IDs, statuses, verbatim claim drawers, finite-\(C\) exclusions, and the explicit distinction between the absolute observable and calibrated functional.

- **The honest negative dockets.** The finite-\(N\) conflict, non-overlapping window, unbalanced-reference obstruction, and finite-coupling HOLD are scientifically valuable and unusually sober. Keep them optional after the verdict, with “conflict” and “HOLD” unchanged.

- **The visual system.** The restrained ledger palette, serif/mono hierarchy, internal horizontal scrolling for wide instruments, theme tokens, and flat evidence panels support the intended traditional-physics register. Repair should compress and relabel, not redesign the page into a game or marketing site.
