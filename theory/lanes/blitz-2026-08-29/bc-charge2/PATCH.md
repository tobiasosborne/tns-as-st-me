# Suggested anchored edits for the orchestrator

No edit outside this lane was made.

## `claims/CLAIMS.md`

In the `| Bc |` row, anchor on the exact sentence:

> the `\|q_hard\|` factor is UNTESTED (all legs `\|q\|=1`) — charge-2 bound-state leg is the next falsifier

Replace it with:

> the charge-two slope falsifier SURVIVED at finite volume: three-magnon ED at `N=100` measured `4.07001 +/- 0.08981` versus `\|q_hard\|/s=4` (`s=1/2`, pre-registered absolute window `0.35`; dense-sector oracle green and hopping mutation red). This is finite-packet evidence only: no `N`/width extrapolation and no charge-two kink-memory test; Bc remains CONJECTURE

Append these lane artifacts to the row's final evidence cell:

> `theory/lanes/blitz-2026-08-29/bc-charge2/RESULT.md`; `test_bc_charge2.jl`; `green.log`; `red-stub.log`; `red-mutation.log`

## `theory/TRIANGLE.md`

Anchor on:

> Every tested leg has unit charge, so the `|q_hard|>1` factor remains open (`tns-ebh`), with a charge-2 bound state the next falsifier.

Replace it with:

> The first charge-two slope falsifier survives at finite volume: an `N=100` three-magnon ED wavepacket experiment gives `4.07001 +/- 0.08981` against `|q_hard|/s=4`. This remains finite-packet evidence, not a proof: an `N`/width convergence study and the charge-two kink-memory experiment are open (`tns-ebh`).
