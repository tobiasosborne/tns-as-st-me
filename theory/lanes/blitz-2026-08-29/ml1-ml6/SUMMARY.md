STATUS: PARTIAL

- `ml1.md`: PROVED-candidate for D6--D8; global ML2 scattering/bound spaces, compatible isometric `W_±`, Cook realization on regular packets, and the single bound fiber are explicit.
- `ml1.md` also derives the `2πδ` one-magnon kernel in every exact D31 register; this remains conditional on D31, not a variational-ansatz theorem.
- `ml6.md`: PROVED-candidate on `(D29-HS-SEP)`; ordered packet limits remove the packet-smeared bound/off-window (R5) Parseval weight before `k_s→0`.
- The surviving real readout is `∫(S_phys−1)dμ=2i\bar k+O_I(ε²)` with `I⋐(0,π)`; the `k_h→0` non-uniformity is explicitly excluded.
- Exact raw bound overlap is `β=2√(1−c²)(cos q−c)/(1−2c cos q+c²)=2k_s+O_I(k_s²)`.
- `ml1_ml6_check.py`: green exit 0; both registered red mutations exit 1; output saved in `ml1_ml6_check.out`.
- Unrestricted ML6 (arbitrary D29/individual unsmeared coefficients/endpoints) remains open, so the lane-wide label is PARTIAL.
- `PATCH.md` targets `claims/CLAIMS.md`, `definitions.md`, `theory/TRIANGLE.md`, and `notation.md` by exact anchors; no shared file was edited.
