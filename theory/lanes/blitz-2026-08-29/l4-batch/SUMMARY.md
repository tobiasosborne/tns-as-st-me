STATUS: PROVED-candidate

- Confirmed `Γ_M` (D31, one vacuum) and `Γ_{M,γ}` (D28, tail family) are disjoint at every use site; retain both spellings.
- Ruled that `ℋ_{0,12}`, `H_{0,12}`, `𝒟_{12}` are first fixed in the two-magnon shard; D31 stays unchanged.
- Prepared the n6 checker copy, overload-7 widening, `{\cal U}_c` row, and a deliberate L2 exception for the converged ML2 shard.
- HEAD table audit catches exactly the six named malformed rows; patched copies pass; the registered unescape mutation fails.
- Patched ML2 checker passes normally and with `python3 -O`; its output is identical to HEAD.
- `PATCH.md` targets `notation.md`, `claims/CLAIMS.md`, `theory/checks/ml2_completeness_check.py`, and `theory/ml2-completeness.md`.
- Full reasoning is in `ADJUDICATION.md`; reproducible output is in `verification.log`.
