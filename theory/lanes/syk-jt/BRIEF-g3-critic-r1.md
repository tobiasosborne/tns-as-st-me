<!-- JOINT CRITIC BRIEF — G3 wave r1 (bd tns-cru). ATTACK, don't
     summarize. YOUR LANE: theory/verdicts/g3-wave-critic-r1.md ONLY
     (+ scratch and mutation COPIES in /tmp; you may run code). No bd
     operations. Work fully autonomously. -->

# G3 wave critic r1: adjudicate construct + obstruct + ward-numeric

Read order: `theory/lanes/syk-jt/observables-spec.md` (binding; note the
iterated-limit bullet) → `theory/lanes/syk-jt/corner-a-precision.md` §4.1
+ G3 + G6 (the problem being decided) → the three briefs
(BRIEF-g3-*.md) → the targets: `g3-construct.md`, `g3-obstruct.md`,
`g3-ward-report.md` + `numerics/src/syk_ward*.jl`,
`numerics/test/test_syk_ward.jl`, `numerics/results/syk_ward.json`.
Ground truth TeX for every leaf: refs/arxiv-1606.01857 (MSY),
refs/arxiv-2301.07257 (Paper.tex), refs/arxiv-2110.05497,
refs/arxiv-2412.14799.

Obligations (recompute, never referee prose):

1. **Obstruct lane.** Independently recompute the three theorem-strength
   claims (G3-UV-NOEXACT, G3-PURE-JT-TRIVIAL, G3-NOT-CONSERVED) leaf by
   leaf: every citation real and saying what is attributed; quantifier
   audit (one-boundary vs relational, exact vs approximate, which
   algebra); check the conditional theorem's hypotheses are declared;
   verify the surviving-object definition (the soft tangent) is
   well-defined at its stated layer and actually survives each attack.
2. **Construct lane.** Audit theorem G3-O the same way; verify its route
   triage doesn't dismiss a route the obstruct lane left open (and vice
   versa); check its KINEMATIC-ONLY classification against the memo's
   G3 classification options.
3. **Ward-numeric.** Run the suite; mutation-test on COPIES (break the
   n=0 exactness gate; corrupt one candidate's hermiticity; inject NaN)
   confirming nonzero exits; recompute at least two defect entries
   independently (small N_m suffices); audit the controls (n=0 machine
   zero; n=1 behavior vs what the constraint structure predicts —
   is the n=1 defect actually small, and if not, what does that do to
   the interpretation of the n=2 rows?); check the candidate operators
   implement what the briefs' families specify.
4. **Cross-consistency (your unique job).** (a) Is the construct lane's
   G3-O consistent in scope with the obstruct lane's ranked
   obstructions — do they jointly cover all routes, or is there a route
   NEITHER lane closed (name it)? (b) Is the obstruct lane's surviving
   soft tangent the same object as the construct lane's kinematic-only
   mode — if not, exactly how do they differ? (c) Does the measured
   O(1) saturating defect quantitatively match what G3-UV-NOEXACT
   predicts (order, temperature dependence), or is agreement merely
   qualitative? (d) The ED window caveat: at N_m ≤ 12 the earlier ED
   rung showed EFT/ED windows barely overlap — does that weaken the
   numeric lane's negative, and how must its scope sentence be worded?
5. **Razor + limit order.** Any frame/label statement not reducing to
   the specified algebra = FATAL. Any permanence/decay claim without
   the declared limit order = objection.

Rulings: per-statement PROMOTE/HOLD with verbatim-ready scoping
sentences for the candidate rows: (i) the n=2 refutation (strong
operational Diff-charge corner A fails at its first non-Möbius mode,
within the stated source-and-size scope); (ii) the surviving weaker
statement (kinematic soft tangent, defined); (iii) any numeric-evidence
row. State explicitly what the refutation does NOT rule out (larger N,
other regulator classes, routes outside the sources). Objection format
as always: location, independent computation, fix demand, surviving
statement, severity. End with PASS or FAIL(ids).
