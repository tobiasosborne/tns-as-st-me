<!-- REPAIR BRIEF r1, lane T1 (analytic). bd tns-62n.
     YOUR LANE: edit theory/lanes/syk-jt/t1-analytic.md and create
     theory/lanes/syk-jt/repair-r1-t1-response.md. Nothing else. No bd
     operations, no CLAIMS edits. Work fully autonomously. -->

# Repair r1 for the analytic lane

Work order: `theory/verdicts/syk-jt-joint-critic-r1.md`. Address EVERY
objection addressed to T1 (1, 2, 3, 7, 8; and your half of 6). Strategy
directive: **downgrade over ambition** — where the critic offers a
surviving weaker statement and the full repair is expensive, take the
downgrade and say so. The critic's recommendation block (§6) is the
promotable shape: use it.

Binding context: `theory/lanes/syk-jt/observables-spec.md` (note the
"Permanent" bullet now carries the adopted iterated-limit convention —
C → ∞ first, 1 ≪ κT ≪ κC).

Per objection:

- **O1 (FATAL, charge sector).** Either (i) recompute protocol (b) with
  the left clock untouched (κ_L = κ_i), using the critic's displayed
  K_LR(T) as the check, with an explicitly implementable equal-(E_L,E_R)
  reference and its preparation stated; or (ii) RETRACT the unbalanced
  protocol-(b) detector claims and restrict every verdict to the
  balanced sector, as the surviving statement licenses. State which.
- **O2 (MAJOR, synchronization).** Declare ONE synchronization event u_*
  and propagate +[2Cκ²]^{-1}∫(u−u_*)P(u)du consistently through the
  comparator, the DC subtraction, the verdict block, and the candidate
  sentence ⟨1⟩5.⟨2⟩6. Note the critic verified your handshake table
  itself is correct — the inconsistency is between (1.2) and (4.6)–(4.10).
- **O3 (MINOR).** Requantify the "iff" as a leading-order criterion with
  the O(C⁻²) caveat.
- **O6 (MAJOR, window).** State the iterated limit convention in the
  verdict block; give the finite-C error structure you can actually
  support and the detector sample-cost statement; do not claim a
  literal T = ∞ detector at fixed N.
- **O7, O8 (MINOR).** Citation pointer (add MSY 586-592) and the missing
  "+" in (3.10).

Rewrite ⟨1⟩5.⟨2⟩6 as the critic's promotable replacement: balanced
pulses (or the repaired u_*-synchronized reference), Δu_rel named a
large-C-first calibrated susceptibility, O(C⁻²)/finite-width errors
retained, the absolute-correlator no-plateau statement separate, the
butterfly-decay clause as its own independent sentence.

Output: the repaired shard (edit in place, keep Lamport structure and
all unaffected step numbers stable) + `repair-r1-t1-response.md` with
one row per objection: id → FIXED / RETRACTED / DOWNGRADED / RESIDUE,
exact edit location. No objection silently ignored.
