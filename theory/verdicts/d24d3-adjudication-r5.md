<!-- ROLE: orchestrator-lane ADJUDICATION of corpus referral bd `tns-iu5`,
     repair round 5 (THE MERGE ROUND), after the FAIL(M1,M2,M3) verdict
     theory/verdicts/d24d3-adjudication-r4-critic.md (0 FATAL, 3 MAJOR,
     8 MINOR, 5 NOTE).
     Work orders: briefs/d24d3-adjudication.md (r1), briefs/d24d3-repair-r2.md,
     -r3.md, -r4.md, briefs/d24d3-repair-r5.md.
     Writable files of this lane: this file,
     theory/verdicts/d24d3-repair-r5-response.md, and
     theory/checks/d24d3_normalization_check.py -- nothing else was edited.
     definitions.md, notation.md and claims/CLAIMS.md are NOT edited here;
     all shared-file text is quarantined in section 5 MERGE PROPOSALS, which
     is ONE coherent application and supersedes r4's section 5 exactly where
     marked (revised spans are reprinted IN FULL; spans the r4 critic ruled
     MERGE are incorporated verbatim, unchanged, and flagged as such).
     THIS DOCUMENT IS A DELTA on theory/verdicts/d24d3-adjudication-r4.md:
     it repairs three sentences and four addresses, supersedes the r4 text
     named in section 0.2, and re-derives NOTHING the r4 critic fenced
     (Y1-Y12), nothing the r3 critic fenced (X1-X15), and nothing from
     r1/r2 (V*, W*).  Read r3, then r4, then this; section 5 below is the
     ONLY text offered for the shared files and it is complete: no span is
     inherited from r4's section 5 except where this file says "r4 verbatim"
     or "r3 verbatim". -->

# r5 adjudication — D24(d) clause 3, the merge round: three sentences and four addresses

**Verdict.  Nothing is redesigned.  The r4 critic ruled the two-anchor
convention WELL POSED, the X2 discharge legitimate, all three of §1.4's costs
actually paid in the merge text, both standing checker complaints genuinely
discharged, and the one stated deviation (m2) ACCEPTED.  None of that is
reopened here.  What r5 does is exactly what the verdict asked for:**

1. **M1** — merged clause 2's "differ by **exactly** `√Z_ρ`" is scoped to the
   LSZ limit and given its finite-`N` value `√(Z_ρ − 2/N)`, so it no longer
   contradicts the sentence it cites; `(b)(ii)` gets the same scope word; and
   the C3 DISPLAY line now prints the LSZ-limit value **and** the finite-`N`
   value obtained by dividing by the **measured** descendant-leg ratio —
   `2.921187`, not `2.828427`, at `2S = 4`, `N = 8` (§1, §6.1).
2. **M2** — the AMP fence ships with **both** of its hypotheses restored, in
   `definitions.md` clause 3b and in the `AMP` DAG row: the **charge-created
   reading of `E^O_desc`** and the **jet-identification bridge (α)**, which
   the same paragraph now labels `(α)` where it first names it (§2, §5.2″,
   §5.3c″).
3. **M3** — `ML5-B` and `D24-VAL` get a supersession pointer in their
   where-proved columns naming r4 §0.2, r5 §0.2 **and the section that is
   actually merged** (r5 §5), so no reader reconstructs the D24(d) text from
   the superseded r3 §5 (§3, §5.3a′, §5.3b′).

**And four addresses are corrected in the merge text itself, since the
orchestrator applies it verbatim** — item 12's second note, §5.4e's
rationale, the `claims/CLAIMS.md` prose site, and `HANDOFF.md:40` as a
**nineteenth** live `𝒮_W` site (§4).  Three of the four were line-number
drift, so **every anchor in §5 is now a quoted string**, with line numbers
demoted to parenthesised hints.  This is not cosmetic: while this round was
being written, `claims/CLAIMS.md` gained two rows and the site the r4 critic
corrected from `82` to **`83`** moved again, to **`85`** (§4.1).  A
line-numbered merge recipe for that file was already wrong twice in two
rounds; a string-anchored one is not.

**Trajectory.  r1 `3/7/6/4` → r2 `1/5/8/5` → r3 `1/5/8/4` → r4 `0/3/8/5` →
r5: all sixteen items dispositioned — 14 FIXED, 2 RECEIVED with no action
required (n2, n4, both on the critic's own direction), 0 deviations,
0 declined**
(table in `theory/verdicts/d24d3-repair-r5-response.md`).  No new physics; no
promotion; no member exhibited; no value installed.  `ML1`, AMP, (α) and
nonemptiness stay open and stay named at every point of use.

---

## 0. Fences honoured, and exactly what r5 supersedes

### 0.1 Not churned (r4-critic §1, Y1–Y12 — certified, untouched here)

The r1 `V*`, r2 `W*`, r3 `X*` and r4 `Y*` fences all stand.  In particular
this round does **not** re-derive, re-argue or re-open:

* **Y1/Y2** — the green banner and all seven red-mode exit paths.  §6.5 below
  reprints the banner **as it now stands**; the seven red-mode exit paths,
  first stderr tokens and numbers are **byte-preserved** and re-run (§6.5).
* **Y3** — the two-magnon replacement gate and its closed form `−2N·Z_ρ`.
* **Y4** — the subsumption proof and the demotion of the fitted-exponent
  comparison to DISPLAY.  *Fenced; the demotion stands.*
* **Y5** — `‖Q^-_q|h⟩‖² = Z_ρN − 2`.  This round **uses** it (M1) and does
  not re-derive it.
* **Y6/Y7** — the fourteen data mutants, and the **ACCEPTED** m2 deviation.
  The per-spin-band alternative is **not** revisited; `ROW_BAND_SANITY = 3.0`
  stands exactly as r4 shipped it.
* **Y8** — the residue in both normalisations at `N = 6`.
* **Y9** — one-commit appliability, and the `notation.md` order
  **b′ → a′ → c → f → e → d**, which the critic verified by application and
  which §5 keeps unchanged.
* **Y10/Y11** — the live anchors and the same-commit obligation addresses,
  except the two the critic corrected (m1, m2), which §4 adopts.
* **Y12** — the §5.2′ diff claim, and the covariance algebra with the
  hard-leg factor carried through the (3a) quotient.

The r4 critic's two rulings in this lane's favour are received and closed:
the **m2 deviation is accepted** (Y7) and the **two-anchor convention is well
posed** (Y12, §9.1).  Neither is re-argued below.

### 0.2 Supersessions — exactly which r4 text r5 replaces

r4 remains the record of the F1 resolution and of the fourth-pass
certificate.  The following r4 statements are SUPERSEDED by this document and
must not be quoted as live:

| r4 location | r4 text (gist) | superseded by |
|---|---|---|
| §5.2′ clause 2 | "The two legs differ by **exactly** `√Z_ρ` ((b)(i))" | §1.1, §5.2″ — `√Z_ρ` **in the LSZ limit**; at finite `N` the descendant-leg ratio is `√(Z_ρ − 2/N)` |
| §5.2′ clause 2 | "This clause is **correct** at every density" | §5.2″ — "**consistent** at every density"; C3 certifies the left-hand side only (r4-critic n1) |
| §5.0′ (b)(ii) | "contributes **exactly** that conversion mismatch, `Z_ρ^{-1/2}`" | §5.0″ — `Z_ρ^{-1/2}` in the LSZ limit, `(Z_ρ − 2/N)^{-1/2}` at finite `N` |
| §5.0′ covariance ¶ | "each is a homogeneous statement in one amplitude" (of clauses 1, 4, 5) | §5.0″ — clause 1 only; clauses 4 and 5 are two-amplitude bounds whose existential `C_I` absorbs `c′/c` (r4-critic m7) |
| §5.0′ covariance ¶ | "Fixing `c′(0)/c = 1` …, **jointly with clause (d)2's residue anchor**, is what makes `𝔞_leg` a number" | §5.0″ — `(b)` alone pins `𝔞_leg` against rescalings; clause (d)2 is a **membership** condition, not a second covariance anchor (r4-critic m8) |
| §5.2′ clause 3b | "…and that value **is refuted** against the ansatz-free two-magnon data" (no hypotheses) | §2, §5.2″ — refuted **on the charge-created reading of `E^O_desc`** and **under the bridge (α)** |
| §5.3c′ `AMP` row | "a value the ansatz-free data refutes at the PRE-REGISTERED band (`--red-halfpower`, deviations 0.42/0.73/1.00 vs band 0.08)" | §5.3c″ — same two qualifiers, and the stale band attribution corrected (r4-critic n3) |
| §5.3a / §5.3b | where-proved columns pointing only at r3 | §5.3a′ / §5.3b′ — supersession pointer to r4 §0.2, r5 §0.2 and r5 §5 |
| §3, site 9 | `claims/CLAIMS.md:82` | §4.1 — the site is prose, not a row; it was `83` when the critic ran and is `85` now.  **String-anchored** |
| §3, "18 live sites" | 18 | §4.1 — **19**: `HANDOFF.md:40` was absent from every r4 list |
| §5.4b′ licensing clause | enumerated list without `HANDOFF.md` | §5.4b″ — `HANDOFF.md` added (r4-critic m2) |
| §5.4e rationale | `theory/ansatz-scattering-2m.md:146` as the live `E_O` site | §5.4e′ — `:170` (`:146` is now a QED line) |
| §5.5′ item 12 | second note at `theory/ansatz-scattering-2m.md:621–631` | §5.5″ item 12 — **string-anchored** at "*multiplier amputation of D24(b), with no extra `ε^{-1}` factor.*" (live `:692`) |
| §6.5 C3 DISPLAY line | `2.828427` printed as "the (b)-asymptotic residue" at `N = 8` | §6.1, §6.5 — the LSZ-limit value **and** the finite-`N` value `2.921187`, each labelled |
| §6.1 `derive_exponent_band` | `require(band > 0.0, …)` | §6.2 — deleted; structurally unreachable (r4-critic m3) |
| §6.4 residual sentence | silent on the uniform channel | §6.3 — the guard is a ratio and is structurally blind to a uniform inflation of the error column |
| §6.5 gate audit | C2's aggregate max only | §6.4 — sub-gate (v)'s **isolated** error reported beside it (r4-critic m5) |
| §1.4 cost list (three costs) | — | §1.4 — a **fourth** cost: the reading was selected by its consequence for emptiness (r4-critic m6) |

### 0.3 Done this round

* **(R5-a)** M1: clause 2 and `(b)(ii)` scoped to the LSZ limit with the
  finite-`N` ratio displayed; the C3 DISPLAY line recomputed against the
  **measured** descendant leg and relabelled (§1, §6.1).
* **(R5-b)** M2: both hypotheses restored in clause 3b and in the `AMP` row;
  `(α)` given its label at first use inside the L4 text (§2).
* **(R5-c)** M3: supersession pointers appended to two where-proved columns
  (§3).
* **(R5-d)** The four addresses corrected **in the merge text**, and every
  anchor in §5 converted to a quoted string (§4).
* **(R5-e)** The eight MINORs and five NOTEs: m1, m2 (addresses, §4); m3, m4,
  m5 (checker, §6.2–§6.4); m6 (fourth cost, §1.4 and clause 2); m7, m8
  (covariance-paragraph wording, §5.0″); n1 ("correct" → "consistent"); n2,
  n4 (recorded, no action); n3 (band attribution corrected); n5 (concurrency
  — answered by the string-anchor conversion, §4.1).
* **(R5-f)** No new symbol, no new gate, no new claim row, no status change.

---

## 1. M1 — "exactly `√Z_ρ`" scoped, and the DISPLAY made true

The critic's finding is accepted in full and without qualification: r4
repaired the vacuum-leg/descendant-leg conflation in `§1.3⟨3⟩1` and in
`(b)(i)`, and then reinstalled it in the two places it newly wrote.  The word
"exactly" in merged clause 2 cites, as its authority, the very sentence that
qualifies it; and the DISPLAY line prints an LSZ-limit number under a
finite-`N` label.  Both are repaired below.  **No conclusion moves** — see
§1.5.

### 1.1 The finite-`N` leg ratio (adopts r4-critic M1(b); nothing new is derived)

**⟨2⟩1. PROVE.**  In the fully polarised spin-`S` register, at even `N`, for
`q ≠ h` commensurate, the ratio between the norm of clause 2's leg and the
norm of a `δ`-normalised asymptotic soft leg is

    ‖Q^-_q|h⟩‖ / ‖|q⟩‖ = √(Z_ρ − 2/N),

equal to `√Z_ρ` only in the `N → ∞` LSZ limit.

**⟨3⟩1.**  `‖Q^-_q|h⟩‖² = Z_ρN − 2` exactly.  Justification: r3-critic X15,
r4-critic Y5 (derived twice and confirmed by two independent ED routes);
gated as **D24N-C8(ii)** with red mode `--red-descnorm` dying at error exactly
`2.000e+00`.  **Not re-derived here.**

**⟨3⟩2.**  `‖|q⟩‖² = N` for the `δ`-normalised asymptotic one-magnon kernel
`|q⟩ = Σ_x e^{iqx}|x⟩`.  Justification: D24N-C8(i); `(b)`'s displayed
convention.

**⟨3⟩3. QED.**  Divide: `√((Z_ρN − 2)/N) = √(Z_ρ − 2/N)`, which tends to
`√Z_ρ` and equals it only in the limit.  ∎

**⟨2⟩2. The consequence for the DISPLAY, at the checker's operating point
(`2S = 4`, `N = 8`, `h = 2π/N`).**  Recomputed this round from scratch, in a
from-scratch two-quantum-sector construction with its own ladder coefficients
(no repo import), and independently of the r4 critic's numbers:

| quantity | value |
|---|---|
| measured `‖Q^-_0|h⟩‖²` | `30.0000000000` (closed form `Z_ρN − 2 = 30`) |
| measured leg ratio `√(Z_ρ − 2/N)` | `1.9364916731` (against `√Z_ρ = 2`) |
| charge-created residue | `5.656854` |
| **LSZ-limit** value `= cc/√Z_ρ` | `2.828427` |
| **finite-`N`** value `= cc/√(Z_ρ − 2/N)` | **`2.921187`** |
| discrepancy | **`3.28 %`** |

The critic's `2.9212` and `3.28 %` reproduce exactly.  So r4's DISPLAY line,
read as its label says, was wrong by `3.28 %` at the `N` it was computed at —
and it was wrong **against this round's own new gate C8(ii)**, which is the
part of the finding that makes it a MAJOR rather than a wording MINOR.

### 1.2 The three text repairs (M1(c), taken verbatim in substance)

1. **Merged clause 2** now reads "*The two legs differ by `√Z_ρ` in the LSZ
   limit — at finite `N` the descendant leg carries `√(Z_ρ − 2/N)` instead
   ((b)(i); named computation D24N-C8(ii)) — and that mismatch is part of
   what clause 3b's constant records*".  The citation and the cited sentence
   now agree.
2. **`(b)(ii)`** now reads "*contributes exactly that conversion mismatch to
   `𝔞_leg` — `Z_ρ^{-1/2}` in the LSZ limit, `(Z_ρ − 2/N)^{-1/2}` at finite
   `N` on the descendant leg (see (i)) — not `Z_ρ^{-1}`*".
3. **The DISPLAY line** now prints **both** values with their scopes named,
   and the finite-`N` one is computed by dividing by the **measured** leg
   ratio `‖Q^-_0|h⟩‖/√N` rather than by a closed form, so the printed number
   is a measurement and not a restatement of C8's formula (§6.1).

Both of the critic's offered options were taken rather than one: the value is
recomputed **and** the label is scoped.  A DISPLAY that prints one number
under two possible readings is what created this defect; printing both
readings, each named, removes the ambiguity instead of relocating it.

### 1.3 Why the conclusions do not move (r4-critic M1(d), adopted verbatim in substance)

`D24(b)` amplitudes are **defined** in the LSZ / infinite-volume register, so
`𝔞_leg^{leg-conv} = Z_ρ^{-1/2}` — the one-step consequence of §1.3⟨3⟩2 of r4
— is stated in exactly the register where the conversion is exact.  The
finite-`N` correction is `1 − √(1 − 2/(Z_ρN))` ≤ `1/(Z_ρN) ≈ 10^{-2}` at the
data's smallest `N = 60`, against refutation margins of `5.2–12.5×` (X2).
Nothing in §1.3, §1.4, the refutation arithmetic, the `AMP` fence or
`D24-VAL` moves.  **What does not survive is the word "exactly" in a
definition that cites its own qualifier as authority, and a DISPLAY label.**
That is the whole of M1, and it is the whole of what §1.2 changes.

### 1.4 The fourth cost (r4-critic m6, adopted)

r4 §1.4 listed three costs and the critic found a fourth that the L4 text did
not disclose: the reading was chosen, among two otherwise available readings,
**by its consequence** — the alternative (clause 2 read against (b)'s
asymptotic leg) is unsatisfiable by an `O`-independent factor at every
`ρ ≠ 1/2`, i.e. it empties the very class the adjudication exists to keep.
That is defensible — the chosen reading is the corpus's own Ward
normalisation, `ml4-ward-reduction.md` (11), whose `(N−2)` denominator the
critic independently confirmed equals `‖Q_0|h⟩‖²` at `Z_ρ = 1` — but it is a
cost, and a reader of clause 2 could not previously tell that the alternative
reading would make the clause unsatisfiable.  Added as:

4. **The reading is selected by its consequence, and the definition now says
   so.**  Between the two available readings of clause 2's residue the
   adopted one is picked because the other empties `𝒮_W(ρ)` off `ρ = 1/2`.
   The selection is disclosed in the L4 text (§5.2″ clause 2's parenthesis),
   not only here, so the choice is auditable at the point of use.

### 1.5 What is NOT changed

The two-anchor convention, the preamble replacement, the invariance list, the
`(3a)` quotient, the one-step derivation of `Z_ρ^{-1/2}`, the refutation
margins, `D24-VAL`, the `AMP` residual content, and every clause the critic
ruled MERGE.  §5.2″ differs from §5.2′ in clause 2 and clause 3b **only**;
§5.0″ differs from §5.0′ in `(b)(ii)` and in two sentences of the covariance
paragraph **only**.  Both diffs are stated line by line in §5.

---

## 2. M2 — the AMP fence ships with both of its hypotheses

The finding is accepted in full.  Two hypotheses were dropped, and the
asymmetry the critic identified is the decisive part of the objection: **the
same paragraph named the bridge for the refutation of the frozen `𝔞_leg = 1`
and omitted it for the refutation of `Z_ρ^{-1/2}`.**  An L4 single source
cannot carry a hypothesis for one of two refutations and drop it for the
other three sentences later.

### 2.1 The two hypotheses, and where each comes from

**(α) the jet-identification bridge.**  The data are two-magnon **phase
slopes**; `𝔞_leg` is a **D24 amputation constant**.  The only thing that
connects them is the corpus's jet identification (`soft-current-recon.md`
(R17), `oracle-bethe.md` O9) — not a PROVED row.  Strip (α) and the
`0.4158/0.7347/1.0033` deviations are deviations of an ML5-B jet from a
measured phase slope, and the comparison is unlicensed.  This is r4's own
`§1.3⟨3⟩3` ("*conditional on the bridge (α), without which nothing is matched
and nothing is refuted*") and r3's `⟨1⟩4.⟨3⟩4`; the `D24-VAL` row carries (α)
as a displayed antecedent and calls it "*not a PROVED row*".  The merge text
must be no weaker than the DAG row two lines above it.

**The charge-created reading of `E^O_desc`.**  In the quotient
`L = E^{(b)}_desc/[(e^{ik}−1)·2iv_h·M_1^O]` both the numerator and the
literal denominator are (b)-normalised; the convention mismatch enters
**only** through the step `E^{(cc)}_desc = R_{cc}·(e^{ik}−1)·(−iχ/v_h)` —
the assumption that the descendant term **is** exactly the propagated
charge-created leg, with no further dressing.  r4 `§1.3⟨3⟩2` states that
assumption, in parentheses; `(b)(ii)` and clause 3b did not.  And r4 `§1.4`
cost 3 says the data (under (α)) "*refutes that reading of `E^O_desc` for any
actual class member*".  So the merge text asserted, as a bare consequence of
the convention, a quantity computed under a hypothesis the same document
reports as refuted for every member of the class the definition is about.
Both halves of the critic's audit are adopted; neither is re-derived.

### 2.2 The repairs

* **Merged clause 3b** — the bridge is **labelled at its first appearance**
  in the same paragraph ("*under the corpus's jet-identification bridge
  **(α)***"), so the later reference resolves inside the L4 text and does not
  depend on a verdict file.  The fence then reads: "*by (b)(ii) a leg
  normalisation alone contributes `Z_ρ^{-1/2}` (in the LSZ limit) … **on the
  charge-created reading of `E^O_desc`** …; **under that reading and under
  the same unproved bridge (α)**, that value is refuted against the
  ansatz-free two-magnon data at the pre-registered band, so AMP requires —
  under those two hypotheses — a second factor `Z_ρ^{-1/2}` from a different
  mechanism*".
* **The `AMP` row's Quantitative fence** — the same two qualifiers, plus
  **n3**: r4's parenthetical attributed to `--red-halfpower` a band it no
  longer prints (its live message names the derived `0.0479`).  The
  deviations do exceed `0.08`, so the claim was true and only the attribution
  was stale; §5.3c″ states the pre-registered `0.08` as the band the
  refutation is quoted at and records that the red mode's own message names
  the tighter derived `0.0479`.

### 2.3 What survives, stated at full strength

The arithmetic is exact and fenced (X2, reproduced at Y2): **under (α) and
the charge-created reading**, `Z_ρ^{-1/2}` misses by `0.4158/0.7347/1.0033`
against a pre-registered `0.08` — `5.2–12.5×`.  The fence's operative
conclusion is untouched: **AMP needs a second, non-leg-normalisation factor
of exactly the computed size, and any proposed proof must be checked against
D24N-C8 or it double-counts the leg.**  What does not survive is the word
"refuted" standing unqualified in an L4 file and an L5 row.

---

## 3. M3 — the DAG points at the text that is merged, not at the text it supersedes

The finding is accepted, and the aggravating circumstance the critic names is
accepted with it: **this is verbatim r2-critic M4 — "the serious one" — one
document over.**  r2-critic M4 required the `corpus-r2.md:174–178`
annotation precisely "*so that ML5-B's where-proved column does not point at
a document certifying a different conclusion*"; r3 §5.5 item 5 accepted it.
The identical argument applies to r3 itself now that r4 §0.2 supersedes
eleven of its statements — including the D24(d) preamble it struck as
**false** and clause 2 **without** its normalisation statement, which is the
text a reader lands on from either row.

The corpus has therefore adjudicated this species of defect twice, and the
lane installed it a third time by marking two rows "r3 verbatim" and not
re-reading their where-proved columns against §0.2.  The rule that follows,
recorded so the next round does not repeat it: **a row marked "verbatim" is
verbatim in its *statement*; its navigation columns must be re-checked
against the current supersession table every round.**

**The repair.**  Both where-proved columns gain one append, and it names
three things rather than one — the two supersession tables *and the section
that is actually merged*, which is the datum a reader reconstructing "what
was committed for `tns-iu5`" actually needs:

    ; superseded in part by `theory/verdicts/d24d3-adjudication-r4.md` §0.2
    (the D24(d) preamble, the clause-2 normalisation, the invariance list,
    the ⟨1⟩4 conversion step) and by
    `theory/verdicts/d24d3-adjudication-r5.md` §0.2 (the clause-2 LSZ scope,
    the clause-3b hypotheses); `theory/verdicts/d24d3-adjudication-r5.md` §5
    is the merged text of record

`r3 §2 ⟨1⟩3` and `ml5-universality.md ⟨1⟩3` — the steps the rows actually
cite — are **not** among the superseded ones (the critic verified this), so
the rows' substance is untouched and the `PROVED` status calls stand (X7,
X8).  Only the navigation is repaired, and navigation is what an L5 file is.

---

## 4. The four addresses, and why §5 is now string-anchored throughout

### 4.1 Line numbers in this merge have now been wrong in three consecutive rounds

The critic verified one-commit appliability by applying all thirteen edits to
scratch copies with an `assert` on every anchor (Y9) — and found four wrong
addresses anyway, three of them line-number drift under concurrent edits.
That is the pattern, not the accident:

* r3 gave `theory/ansatz-scattering-2m.md:141` for the live `E_O` site; r3's
  critic corrected it to `:146`; r4 shipped `:146`; **live it is `:170`**.
* r3 §5.5 item 12 addressed the `D24(b)` citation at `:614, 621–631`; the r4
  critic found it at `:673–692` after that shard's r6 landed mid-review;
  **live the sentence ends at `:692`**, and `:614` now lands on an unrelated
  Justification line.
* The r4 critic corrected `claims/CLAIMS.md`'s second bare `𝒮_W` from `82` to
  **`83`**.  Re-grepped live for this round: it is at **`85`** — two rows
  (`LR1-GEN`, `LR-D16-EDW`) were added to that file while this round was
  being written, and are now committed (`137f922`).  **The corrected number
  was stale before the correction could be applied** — and the r4 critic's
  own Y9 application test, which passed on every anchor, would not have
  protected the commit, because it tested the file as it then stood.

So the brief's instruction is adopted as a rule, not as four edits: **every
anchor in §5 is a quoted string.**  Line numbers survive only as
parenthesised "(live at `:NNN` when this was written)" hints, and no
application step depends on one.  The three `definitions.md` REPLACEs, the
six `notation.md` edits and the four `claims/CLAIMS.md` edits each name their
first and last line as literal text; the CLAIMS edits match on row label,
which r4 already offered and which the critic endorsed (n5).  Anchor
uniqueness was re-verified live for every string in §5 (§5, Mechanics).

### 4.2 The nineteenth site

`HANDOFF.md:40` — "*(3) **Frozen `𝒮_W` empty for `ρ≠1/2`***" — is a live bare
use and appears in no r4 list.  It is not among the sites §5.4b′'s licensing
clause enumerates, so on the clause's own reading it would be left
unlicensed; it is saved only incidentally, because r3 §5.5 item 8 rewrites
that very line.  Both of the critic's offered fixes are taken: `HANDOFF.md`
is added to the licensing clause's enumerated list **and** §5.5″ item 8
records that the rewrite removes this occurrence, so the licence is a
belt-and-braces and not a dependency on the rewrite's exact wording.

### 4.3 The rename disposition at all nineteen sites (supersedes r4 §3)

Fresh whole-repo grep this round, excluding verdict files, briefs and this
lane's documents.  **Disposition is unchanged from r4** — five renames, the
rest licensed — only the enumeration is corrected and re-anchored.

| # | site | anchor string (line numbers are hints only) | disposition |
|---|---|---|---|
| 1 | `theory/TRIANGLE.md` (`:24`) | `Universality of the soft coefficient is scoped to` | RENAME (r3 §5.5 item 2, ruled MERGE) |
| 2 | `theory/TRIANGLE.md` (`:215`) | `and membership in `𝒮_W` (D24(d)), outside which universality is` | RENAME (same) |
| 3 | `theory/TRIANGLE.md` (`:254`) | `is conjectured universal on the Ward-covariant, no-contact class` | RENAME (same) |
| 4 | `theory/TRIANGLE.md` (`:257`) | `no-contact conditions 4--5 of` | RENAME (same) |
| 5 | `theory/TRIANGLE.md` (`:528`) | `decomposition; microscopic `𝒮_W` membership; and ML6 remainder` | RENAME (same) |
| 6 | `definitions.md` (`:916`) | `## D24 (hard sources, the contact first jet, and the no-contact class` | LICENSED bare (§5.4b″) |
| 7 | `definitions.md` (`:1004`) | `tensors within the Ward-covariant no-contact class` | LICENSED bare |
| 8 | `claims/CLAIMS.md` `S-general` row (twice in the row) | `| S-general | Lattice soft theorem` | LICENSED bare |
| 9 | `claims/CLAIMS.md` corner-implication **prose**, not a row (`:82` in r4, `:83` per the critic, **`:85` live**) | `at `n≥2`, the exhaustive LSZ decomposition, and microscopic` | LICENSED bare |
| 10–14 | `theory/ml5-universality.md` (`:133, 141, 152, 159, 197`) | `let a source class`, ``for every `O∈𝒮_W`.``, ``**PROVED.** Every `O∈𝒮_W` obeys``, `of the hard process within`, `A nonempty class` | LICENSED bare |
| 15–18 | `paper/main.tex` (`:245, 252, 542, 556`) | `no-contact class $\mathcal{S}_W$ of Appendix~B`, `in $\mathcal{S}_W$; Appendix~B states`, `holds on the source class $\mathcal{S}_W$ defined by five`, `microscopic membership in $\mathcal{S}_W$` | LICENSED bare |
| **19** | **`HANDOFF.md`** (`:40`) | `**Frozen 𝒮_W empty for` | **LICENSED bare (NEW) — and rewritten by §5.5″ item 8** |

(The two `notation.md` occurrences are registry rows, not live uses; they are
edited by §5.4c and §5.4b″.  r4's `main.tex:245/252/542/556` are here given
their strings; the r4 table's `:542/:556` were listed as `:542`/`:556` and
re-verified live this round.  Two further bare uses sit in
`theory/ansatz-scattering-2m-r4-response.md` and
`theory/ansatz-scattering-2m-r5-response.md`: **verdict records are frozen
historical documents and are deliberately not renamed or edited**, and
frozen lane records — verdicts and `*-response` documents — are outside the
registry's scope and are never renamed.)

### 4.4 The other two addresses

* **§5.4e's rationale** cites the live `E_O` sites for overload rule 8.
  `theory/ansatz-scattering-2m.md:146` is now a `**⟨2⟩4. QED.**` line; the
  live `E_O` occurrence is at `:170` (`` `Ψ_O(Y):=tr_{d^{w_O}}[(O⊗1)V_{w_O}YV_{w_O}^†]=E_O(Y)` ``).
  §5.4e′ reprints the rationale with `:170` and with the string quoted, so
  the next drift does not invalidate it.  **The fenced text of the rule-8
  amendment itself is unchanged and stays r3 verbatim** — only the rationale
  prose around it moves.
* **§5.5″ item 12's second note** targets the ⟨2⟩5 step of
  `theory/ansatz-scattering-2m.md` that uses the D24(b) amputation.  It is
  **string-anchored** at "*multiplier amputation of D24(b), with no extra
  `ε^{-1}` factor.*" — a unique line, live at `:692`, ending the paragraph
  the note attaches to.  The r4 address `:621–631` is stale by ~50 lines and
  that shard is under active edit by another lane (its r6 verdict landed
  during the r4 review), so the note is applied **after the matched string**
  and never by line number.  The ML4 half of item 12
  (`theory/ml4-ward-reduction.md:63–68`) was re-verified exact (Y11) and is
  likewise given its string.

---

## 5. MERGE PROPOSALS (r5-final — supersedes r4 §5 exactly where marked)

*This lane edited no shared file.  The orchestrator applies the following as
**ONE commit**.  Spans marked ″ are REVISED this round and reprinted in full;
spans marked ′ are r4's, reprinted where this round changes them; spans marked
"r3 verbatim" / "r4 verbatim" are incorporated unchanged, and are exactly the
spans the r4 critic ruled MERGE.  A partial application is not offered:
§5.1's `𝔞_leg` and §5.2″'s clause 3b are meaningless without §5.0″'s
convention, and §5.3's rows quote both.*

**Anchoring policy (r4-critic m1, m2, n5 — the durable fix).**  Every anchor
below is a **quoted string**.  Line numbers appear only as parenthesised
hints of where the string was found when this was written; **no application
step may depend on one**, because three of the four addresses the r4 critic
corrected were line-number drift and one of those corrections went stale
again before it could be applied (§4.1).  Every string below was verified
**unique** in its target file this round.

**Mechanics — `definitions.md` (three REPLACEs, descending; unchanged from
r4 except for the anchoring policy).**

| # | REPLACE from (first line, literal) | through (last line, literal) | source |
|---|---|---|---|
| (a) | `**(d) The Ward-covariant no-contact class` | `is part of the ML5-B future work.` | §5.2″ (hint: `:952–973`) |
| (b) | `**(c) The contact first jet.**` | `factorisation with the same stated norm control.` | §5.1, r3 verbatim (hint: `:931–950`) |
| (c) | `**(b) Amputated amplitudes.**` | ``waves are generalised kernels inside `C_c^∞` packets (D3(a), D12(a′)).`` | §5.0″ (hint: `:923–929`) |

Descending order is what keeps the later anchors valid; with string matching
the order is belt-and-braces rather than load-bearing, and it is kept.

**Mechanics — `notation.md` (six edits, in the r4 order `b′ → a′ → c → f →
e → d`, which the r4 critic verified by application — Y9 — and which is NOT
changed here).**

1. **§5.4b″** — REPLACE the row beginning ``| `𝒮_W` | the five-condition
   Ward/LSZ/no-contact source class`` (hint: `:258`);
2. **§5.4a′** — ADD the four new rows immediately after that replaced row;
3. **§5.4c** — inside the row beginning ``| `𝖲(k_s;{q_i,v_i})` | Corner-C
   proposed soft multiplier`` (hint: `:244`), replace the substring
   ``on the D24(d) class `𝒮_W`;`` (with its trailing semicolon — verified
   unique in the file at this point of the sequence, i.e. after edit 1 has
   replaced the registry row) by ``on the D24(d) class `𝒮_W(ρ)`;``;
4. **§5.4f** — INSERT overload rule 13 after the line
   ``    tensor (`TRIV₂`); no other reading of `T` takes that position.``
   (rule 12's last line, hint: `:145`), keeping the blank line before
   `All other symbols are unique.`;
5. **§5.4e′** — APPEND to overload rule 8, the block beginning
   ``8. **`E`** — the transfer map and its contractions carry an *operator or``
   and ending ``   `→E_C`, or `E_C⊗E_C^*` (D19, D21).`` (hint: `:123–130`);
6. **§5.4d** — APPEND to overload rule 2, the block beginning
   ``2. **`χ`** — bond dimension (D1(b)) *versus* `χ := sgn(v_h − v_s)` in the``
   and ending ``   `χ` as a sign into any other shard.`` (hint: `:91–98`).

Applying them in the r3 written order `a→f` moves rule 8 after the rule-2
append and breaks two anchors (r3-critic m3); this order is the fix and is
retained verbatim from r4.

**Mechanics — `claims/CLAIMS.md` (four edits, matched on ROW LABEL — never on
line number; that file gained two rows while this round was written, §4.1).**
REPLACE the row beginning `| ML5-B |` ← §5.3a′ → ADD `| D24-VAL |` after it ←
§5.3b′ → ADD `| AMP |` after that ← §5.3c″ → REPLACE the row beginning
`| ML5-A |` ← §5.3d.  Row labels are unique in that file (verified this
round).

### 5.0″ `definitions.md` — D24(b) (REVISED from r4 §5.0′: `(b)(ii)` LSZ scope [M1]; covariance paragraph [m7, m8].  Every other line byte-identical to r4 §5.0′)

**REPLACE** (from `**(b) Amputated amplitudes.**` through
``waves are generalised kernels inside `C_c^∞` packets (D3(a), D12(a′)).``):

```
**(b) Amputated amplitudes, and the normalisation they are measured in.**
For a source `O`, `M_1^O(h)` is its amputated one-hard amplitude and
`M_2^O(k,h)` its connected hard-plus-soft amplitude in the physical channel;
both are linear in `O`, and both are measured as multipliers in `L²(I,dh)`
for a fixed hard window `I = [a,b] ⋐ (0,π)`.  Remainders are measured in
`‖R_{S2}(k)‖_{L²(I)} ≤ C_I|k|²‖M_1^O‖_{L²(I)}`, or, for
`f_ε(k) = ε^{-1/2}f(k/ε)`, as `O(ε²)` in the product packet norm.  Plane
waves are generalised kernels inside `C_c^∞` packets (D3(a), D12(a′)).

*Amputation and leg-normalisation convention.*  Both amplitudes are taken
against asymptotic one-magnon kernels normalised by `⟨k|k′⟩ = 2πδ(k−k′)`,
inside the corpus's packet discipline (D3(a); the `ℓ¹∩BV` packet class of
D12(a′)).  That kernel is *constructed* in the fully polarised spin-`S`
register, where it is `|k⟩ = Σ_x e^{ikx}|x⟩` (named computation D24N-C8 in
`theory/checks/d24d3_normalization_check.py`, an exact one-magnon
eigenvector of `H_S`); in any other register the existence of the
asymptotic one-magnon kernel is claim `ML1` (CONJECTURE), and every
statement about the (d)3b constant `𝔞_leg(ρ)` is scoped to a register
where the kernel exists.  The **same** amputation is applied to the
**same** hard leg in both.  The soft leg enters `M_2^O` as **one
additional `δ`-normalised asymptotic magnon of momentum `k`, carrying
unit leg weight** — not as a charge-created or current-created vector.
This convention is strictly *narrower* than the ML4-admissible analytic
amputation of `theory/ml4-ward-reduction.md` (which admits any bounded
`C¹` multiplier family at fixed `N`): here every soft-leg envelope `λ(k)`
must have `λ(0) = 1`.

This is a *convention*, not a theorem, and it is load-bearing.  A change of
normalisation `M_1^O ↦ c\,M_1^O`, `M_2^O(k,·) ↦ c′(k)\,M_2^O(k,·)` with
`c, c′(k) ≠ 0` (necessarily independent of `O`, by linearity above) leaves
clauses (d)1, 4 and 5 invariant — clause 1 is a homogeneous statement in one
amplitude, while clauses 4 and 5 are two-amplitude bounds whose existentially
quantified constant `C_I` absorbs `c′/c` — while clause (d)2, whose residue
is quoted against the charge-created soft leg (see (d)2), transforms as
`c′/c`, and the (d)3a external flux factor and the (d)3b amputation constant
transform as `L(k,h) ↦ (c′(k)/c)L(k,h)` and `𝔞_leg ↦ (c′(0)/c)\,𝔞_leg`.
Fixing `c′(0)/c = 1` as above is what pins `𝔞_leg` against those rescalings,
and (b) supplies it alone.  Clause (d)2 is *not* a second covariance anchor:
read against the charge-created leg it is a cross-normalisation **membership**
condition on the source — it ties `E^O_desc` to `2i v_h M_1^O` — and it is
that, not a second fixing of the covariance freedom, which makes `𝔞_leg` a
number rather than a name.  Without both, "`𝔞_leg = 1`" and
"`𝔞_leg = 1/Z_ρ`" are statements about a convention and not about a model.
What remains free does not move `𝔞_leg`: rescaling the source (both
amplitudes are linear in `O`); any hard-leg amputation convention applied
identically in both (it cancels in the (d)3a quotient); and any soft-leg
envelope `λ(k)` with `λ(0) = 1`.

Two consequences are recorded because they are used below.  (i) In the
fully polarised spin-`S` ferromagnet the **charge-created** soft leg is
*not* this convention's soft leg: `S^-_x|Ω⟩ = √(Z_ρ)\,|x⟩` on normalised
one-flip states, hence `Q^-_k|Ω⟩ = √(Z_ρ)\,|k⟩` exactly on the vacuum
(named computation D24N-C8, verified against the Hamiltonian's own
one-magnon eigenvector), the two legs coinciding only at `ρ = 1/2`; on a
descendant (one-hard-magnon) state the same conversion holds per site
only up to `O(1/(Z_ρN))` — `‖Q^-_q|h⟩‖² = Z_ρN − 2` exactly for `q ≠ h` (D24N-C8) —
becoming exactly `√Z_ρ` in the LSZ limit.  (ii) Because clause (d)2's
residue is quoted against the charge-created leg while the (d)1 summand
`E^O_desc` is measured against this convention's asymptotic leg, a
soft-leg normalisation argument alone contributes exactly that conversion
mismatch to `𝔞_leg` — `Z_ρ^{-1/2}` in the LSZ limit, and
`(Z_ρ − 2/N)^{-1/2}` at finite `N` on the descendant leg (see (i)) — and not
`Z_ρ^{-1}`; see the fence on lemma AMP in (d)3b.
```

*(Diff against r4 §5.0′, for the reviewer: exactly two hunks.  (1) The
covariance paragraph's homogeneity clause is split — clause 1 homogeneous in
one amplitude, clauses 4 and 5 two-amplitude bounds whose existential `C_I`
absorbs `c′/c` (m7) — and the "jointly with clause (d)2's residue anchor"
sentence is replaced by the two-jobs statement: `(b)` alone pins `𝔞_leg`
against rescalings, clause (d)2 is a membership condition (m8).  (2) `(b)(ii)`
gains the LSZ scope word and the finite-`N` value (M1).  Paragraphs 1, 2, the
free-freedom sentence and `(b)(i)` are byte-identical.)*

### 5.1 `definitions.md` — D24(c)  (r3 verbatim — ruled MERGE by two critics; unchanged)

Apply `theory/verdicts/d24d3-adjudication-r3.md` §5.1 unchanged, byte for
byte, between the anchors in the Mechanics table.  Nothing in M1, M2 or M3
touches D24(c).

### 5.2″ `definitions.md` — D24(d) (REVISED from r4 §5.2′: clause 2 [M1, n1, m6]; clause 3b [M2].  Every other line byte-identical to r4 §5.2′, hence to r3 §5.2 outside r4's own two clauses)

**REPLACE** (from `**(d) The Ward-covariant no-contact class` through
`is part of the ML5-B future work.`):

```
**(d) The Ward-covariant no-contact class `𝒮_W(ρ)`.**  Fix a broken-symmetry
tail density `ρ := ω_α(S^z) > 0` (`notation.md`'s `ρ`-row), and write
`Z_ρ := 2ρ` for the order-parameter density (`notation.md`'s `Z_ρ`-row; in
an su(2) model with a fully polarised α tail, `Z_ρ = ω_α([S^+_x, S^-_x])` —
a special case, not a membership condition).  Under (S) at both tails,
D26(INT), and the antisymmetric tail pair `ω_β(S^z) = −ω_α(S^z) = −ρ` —
the load-bearing hypothesis of claim `M-IDX-density` — `2ρ ∈ ℤ`, so `Z_ρ`
is then a positive integer.  `ρ = 0` is excluded: D26 admits it, but there
is then no broken order parameter and no type-B soft leg.  In the fully
polarised spin-`S` ferromagnet `ρ = S`.  Amplitudes below (`M_1^O`,
`M_2^O`, and the clause-1 summand `E^O_desc`) are in the normalisation
convention of (b); clause 2's residue display is quoted in the
charge-created normalisation of `ml4-ward-reduction.md` (11), as stated in
that clause; clause 3b is empty without (b).  `𝒮_W(ρ)` is the class of
sources satisfying all five of (clause 3 has two parts; the *count* of
conditions is deliberately unchanged, so `paper/main.tex:244`
"five-condition" and the audit rows that quote it stay correct):
1. *Exhaustive normed LSZ decomposition* — in `L²(I)`, `M_2^O` is exactly
   the sum of the descendant external-leg term `E^O_desc`, the
   orthogonal-current term, and the direct source/contact term named below;
   there is no additional reduced term, and the equality holds in the same
   packet norm as (b).
2. *Ward covariance* — the descendant current residue, **quoted against
   the charge-created soft leg `Q_0|h⟩` — the normalisation of
   `ml4-ward-reduction.md` (11) — and not against (b)'s asymptotic
   leg**, factorises as `2i v_h M_1^O(h)`.  The two legs differ by
   `√Z_ρ` **in the LSZ limit**; at finite `N` the descendant leg carries
   `√(Z_ρ − 2/N)` instead, since `‖Q^-_q|h⟩‖² = Z_ρN − 2` exactly for `q ≠ h`
   ((b)(i); named computation D24N-C8(ii)).  That mismatch is part of
   what clause 3b's constant records: against (b)'s asymptotic leg the
   same residue reads `2i v_h M_1^O(h)/√Z_ρ` in that limit, the two
   readings coinciding only at `ρ = 1/2`.  (This clause is *consistent*
   at every density: its left-hand side is exactly `Z_ρ`-linear on the
   fully polarised spin-`S` tail, `⟨h|Q_0^†J^-_0|h⟩ = 2i v_S(h)` with
   `v_S(h) = Z_ρ J\sin h`; frozen `soft-current-recon.md` (R14) is its
   `Z_ρ = 1` reading — named computation D24N-C3 in
   `theory/checks/d24d3_normalization_check.py`, which displays the
   residue in both normalisations and in both scopes.  Whether any given
   `O` also makes the right-hand side match is a membership question,
   which no computation here decides.  The alternative reading, against
   (b)'s asymptotic leg, would be unsatisfiable by an `O`-independent
   factor at every `ρ ≠ 1/2` — that is why this normalisation and not
   that one.)
3. *Kinematic LSZ normalisation*, in two parts:
   **(3a) hypothesis** — the external flux factor
   `L(k,h) := E^O_desc(k,h)/[(e^{ik}−1)\,2i v_h\,M_1^O(h)]` is well
   defined by clauses 1--2 for `k ≠ 0` at every `h` with `M_1^O(h) ≠ 0`;
   the hypothesis is that it extends to a process-independent, uniformly
   `C¹` function on `{|k| ≤ ε} × I`, and `L(0,h)` denotes that extension's
   value at `k = 0`.
   **(3b) the profile of `L(0,h)`, and the OPEN amputation constant** —
   `v_h\,L(0,h)/χ(h,0)` does not depend on `h` on `I`; equivalently
   `L(0,h) = 𝔞_leg(ρ)·(−iχ(h,0)/v_h)` for some nonzero constant
   `𝔞_leg(ρ)`.  **What this clause asserts is that `h`-profile — the
   reciprocal LSZ energy denominator `[ω(h+k)−ω(h)]^{-1}k`, the corpus's
   fixed provenance for `L` (`soft-current-recon.md` ⟨1⟩7,
   `paper/main.tex:524--527`, `soft-index-b.md:808`) — and no value.**  By
   (3a) `L` is a single class-level function, so `𝔞_leg(ρ)` is a datum of
   the class `𝒮_W(ρ)` and not of any individual source; it is defined
   only when that class is nonempty, and only in a register where the (b)
   asymptotic one-magnon kernel is constructed (currently the fully
   polarised spin-`S` family, D24N-C8; off that family, kernel existence
   is claim `ML1`, CONJECTURE).  **This definition fixes no value of
   `𝔞_leg(ρ)` at any density, `ρ = 1/2` included.**  As frozen, this clause
   stipulated `𝔞_leg = 1`, which under the corpus's jet-identification
   bridge **(α)** — the identification of the D24(d) soft multiplier's
   jet with the two-body physical phase jet, an unproved bridge, row
   `D24-VAL` — contradicts PROVED `S2-2body-S` at every `ρ ≠ 1/2` (bd
   `tns-iu5`); that stipulation is withdrawn.  Two candidate readings are
   carried in `claims/CLAIMS.md` and neither is part of this definition:
   the conditional matched value `𝔞_leg(ρ) = 1/Z_ρ` (row `D24-VAL`,
   proved as an implication from the bridge plus class membership), and
   the open lemma **AMP** of the same value from a soft-leg amputation
   mechanism (row `AMP`, CONJECTURE).  Fence on AMP: by (b)(ii) a leg
   normalisation alone contributes `Z_ρ^{-1/2}` — the exact conversion,
   in the LSZ limit, between clause 2's charge-created residue
   normalisation and (b)'s asymptotic leg — **on the charge-created
   reading of `E^O_desc`**, i.e. on the reading that the clause-1
   descendant term is exactly the propagated charge-created leg with no
   further dressing; and **under that reading and the same unproved
   bridge (α) named above**, that value is refuted against the
   ansatz-free two-magnon data at the pre-registered band, so AMP
   requires — under those two hypotheses — a second factor `Z_ρ^{-1/2}`
   from a different mechanism
   (`theory/verdicts/d24d3-adjudication-r3.md` ⟨1⟩4; one-step form
   `theory/verdicts/d24d3-adjudication-r4.md` §1.3).  At `ρ = 1/2` the two
   candidate readings and the retired stipulation all coincide at `1`, so
   the D6 displays of the soft coefficient `2` remain numerically
   consistent with this clause; those displays rest on the contact/oracle
   route (`oracle-bethe.md` O7–O9; `paper/main.tex:515–520`), not on this
   clause;
4. *Reduced-channel regularity* — the orthogonal current channel is
   `O_{L²(I)}(k²)` in the stated target limit, with the relative norm
   bound of (b).  Fixed-volume ML4 does not by itself supply a
   volume-uniform bound;
5. **No direct soft contact** — the amputated source commutator/contact
   term is bounded by `C_I|k|²‖M_1^O‖_{L²(I)}`.

Hypotheses 1 and 5 are the extra LSZ/contact content beyond the fixed-volume
Ward reduction, and by (c) the zero-intercept and zero-first-jet conditions
are necessary, not cosmetic.  Symmetry-generated external-leg insertions are
the intended seed, but **no nontrivial microscopic class has been proved to
satisfy all five conditions**; nonemptiness in the target scattering
register is part of the ML5-B future work.  The `tns-iu5` adjudication of
clause 3 removed a *proof* that `𝒮_W(ρ)` is empty for `ρ ≠ 1/2` — as frozen,
clause 3 forced soft slope `2` at every density, contradicting PROVED
`S2-2body-S` given the jet-identification bridge — but it supplies no member
and fixes no value of `𝔞_leg(ρ)`, so nonemptiness stays open at every `ρ`
(bd `tns-axg`).
```

*(Diff against r4 §5.2′, for the reviewer: exactly four hunks, all inside
clauses 2 and 3b.  Clause 2: "exactly `√Z_ρ`" → `√Z_ρ` in the LSZ limit with
the finite-`N` ratio `√(Z_ρ − 2/N)` and its C8(ii) citation (M1); "correct"
→ "*consistent*", with the certificate's one-sidedness stated (n1); the
selection-by-consequence clause added (m6).  Clause 3b: the bridge is
labelled `(α)` where the paragraph first names it, and the AMP fence carries
both hypotheses (M2).  Clauses 1, 3a, 4, 5, the preamble and the closing
paragraph are byte-identical to r4, which the r4 critic diffed against r3 and
found byte-identical there except in the four places r4 declared — so the
three-round diff chain is complete and checkable.)*

### 5.3 `claims/CLAIMS.md` (four edits, matched on row label)

*L5 compliance note, unchanged from r3: the file header fixes the
authoritative statuses as `PROVED | SKETCH | CONJECTURE | REFUTED` and puts
empirical/conditional/under-review qualifiers in prose.  No status changes in
this commit.*

**5.3a′ — REPLACE the row beginning `| ML5-B |` — REVISED (M3: supersession
pointer appended to the where-proved column; statement column byte-identical
to r3 §5.3a):**

```
| ML5-B | Conditional implication on the five-condition D24(d) class `𝒮_W(ρ)`, including the exhaustive normed LSZ decomposition.  The implication is proved; its displayed conclusion is `M_2^O(k,h) = 2i𝔞_leg(ρ) χ k M_1^O(h) + O_{L²(I)}(k²)`, with `𝔞_leg(ρ)` the D24(d)3b soft-leg amputation constant.  **This conclusion contains no number at any density, `ρ = 1/2` included:** D24(d)3b fixes no value of `𝔞_leg`, so ML5-B predicts a factorisation SHAPE — linear in `k`, proportional to `M_1^O`, with a class constant — and not a coefficient.  The number `2` was never a consequence of this row; it was a consequence of this row plus the frozen D24(d)3 stipulation `𝔞_leg = 1`, which is withdrawn.  Corpus displays of the soft coefficient `2` rest on the contact/oracle route (`oracle-bethe.md` O7–O9, `paper/main.tex:515–520`), which does not use D24(d).  **Re-scoped 2026-08-29 (bd `tns-iu5` r3):** the frozen clause forced soft slope `2` at EVERY tail density, contradicting PROVED `S2-2body-S` (`1/S`) given the jet-identification bridge — frozen `𝒮_W` provably empty for `ρ ≠ 1/2`.  Clause 3 is now split: 3a keeps the process-independence/`C¹`-extension hypothesis with `L` DEFINED as the descendant quotient (`k ≠ 0`); 3b asserts only the `h`-profile of `L(0,h)` and leaves `𝔞_leg(ρ)` open.  The two candidate values are carried as rows D24-VAL (conditional, proved as an implication) and AMP (CONJECTURE).  The proof of the implication is unchanged apart from carrying `𝔞_leg` as a nonzero constant factor (⟨2⟩1.⟨3⟩2 needs only `𝔞_leg ≠ 0` and `v_h` bounded below on `I`), so the corpus-r2 promotion is reparametrised, not reopened.  Nonemptiness and microscopic membership remain CONJECTURE/future work bd `tns-axg`, at every `ρ`. | PROVED | ML4-A, ML5-A, D24(d) | theory/ml5-universality.md ⟨1⟩3; PROVED per corpus-r2.md adjudication (conditional implication) — see the r3 annotation appended at corpus-r2.md:174–178, which records that the multiplication certified there survives verbatim with `𝔞_leg` carried; clause-3 repair adjudicated in theory/verdicts/d24d3-adjudication-r3.md (bd tns-iu5 r3); superseded in part by `theory/verdicts/d24d3-adjudication-r4.md` §0.2 (the D24(d) preamble, the clause-2 normalisation, the invariance list, the ⟨1⟩4 conversion step) and by `theory/verdicts/d24d3-adjudication-r5.md` §0.2 (the clause-2 LSZ scope, the clause-3b hypotheses); `theory/verdicts/d24d3-adjudication-r5.md` §5 is the merged text of record | theory/checks/ml4_check.py local obstructions only (`𝔞_leg`-blind: both D24(e) counterexamples have `M_1^O = 0`); theory/checks/d24d3_normalization_check.py D24N-C1–C3 test the kinematics of the conclusion, not its constant |
```

*(Note, unchanged from r3: `S2-2body-S` is deliberately NOT added to ML5-B's
depends-on — with the value retired, ML5-B's conclusion does not reference
it; the dependency belongs to `D24-VAL`.  Verified by the r2 and r4 critics,
X8.)*

**5.3b′ — ADD a new row `D24-VAL` immediately after the `ML5-B` row —
REVISED (M3: same supersession pointer; statement column byte-identical to
r3 §5.3b):**

```
| D24-VAL | Conditional matched value of the D24(d)3b amputation constant, in the D24(b) normalisation convention.  GIVEN (α) the jet-identification bridge — the D24(d) soft multiplier's jet equals the two-body physical phase jet (corpus identification via soft-current-recon.md (R17) and oracle-bethe.md O9; NOT a PROVED row) — and (β_S) existence of a member `O ∈ 𝒮_W(ρ)\|_{ρ=S}` with `M_1^O ≠ 0` in the fully polarised spin-`S` register (written `𝒮_W(ρ)\|_{ρ=S}`, not `𝒮_W(S)`, to keep the `ρ`-vs-`s` fence visible at the point of use; and note that the `ρ` here is the D24(d) tail density of `notation.md`'s `ρ`-row, NOT the file-local double-occupancy coefficient `ρ` of theory/spin-s-twomagnon.md ⟨1⟩1–⟨1⟩2, which is the shard `S2-2body-S` lives in): THEN, for each such `S` separately, `𝔞_leg(S) = 1/(2S) = 1/Z_ρ`, i.e. `L(0,h) = −iχ/(2ρ v_h)` and ML5-B jet `χ/ρ`, at `S ∈ {1/2, 1, 3/2, 2}`.  Within the family `𝔞_leg = Z_ρ^{-p}` the exact law `(2S)^p = 2S` forces `p = 1` at any single `2S ≠ 1` (the `S = 1/2` point is degenerate; the four densities are four points of one analytic curve, not four independent measurements).  **The implication is a theorem; its antecedents are not.**  It is currently VACUOUS-OR-UNKNOWN: (β_S) is open at every `S` (bd `tns-axg`) and (α) is unproved, so the row constrains future work — any member ever exhibited must have this value — rather than supplying evidence now.  It cannot be upgraded past that reading until `tns-axg` exhibits a member or AMP closes.  Extension to non-half-integer `ρ` is hypothesis, not consequence. | PROVED | D24(b), D24(d), S2-2body-S, ML5-B | theory/verdicts/d24d3-adjudication-r3.md §2 ⟨1⟩3; superseded in part by `theory/verdicts/d24d3-adjudication-r4.md` §0.2 (the D24(d) preamble, the clause-2 normalisation, the invariance list, the ⟨1⟩4 conversion step) and by `theory/verdicts/d24d3-adjudication-r5.md` §0.2 (the clause-2 LSZ scope, the clause-3b hypotheses); `theory/verdicts/d24d3-adjudication-r5.md` §5 is the merged text of record | theory/checks/d24d3_normalization_check.py D24N-C6 tests the numerical consistency of the CONSEQUENT with the ansatz-free two-magnon slopes, at a band derived from the data's own quoted error; neither antecedent is tested by any gate, and no gate bears on `L`, `E^O_desc` or membership |
```

**5.3c″ — ADD a new row `AMP` immediately after the `D24-VAL` row — REVISED
(M2: both hypotheses restored in the Quantitative fence; n3: the stale band
attribution corrected.  All other text byte-identical to r4 §5.3c′,
including M5's quantifier repair, which is not reopened):**

```
| AMP | Soft-leg amputation lemma: for every `ρ` at which `𝒮_W(ρ) ≠ ∅` AND the D24(b) asymptotic one-magnon kernel exists (currently: the fully polarised spin-`S` family, named computation D24N-C8; off that family kernel existence is claim ML1, CONJECTURE), charge-created soft-leg amputation contributes the per-site order-parameter density `Z_ρ = 2ρ` to the external flux, i.e. `𝔞_leg(ρ) = 1/Z_ρ` — WITHOUT the D24-VAL bridge (α); dropping (α) is what distinguishes this row from D24-VAL, so the DAG does not double-count.  Like D24-VAL it is currently VACUOUS-OR-UNKNOWN: nonemptiness is open at every `ρ` (bd `tns-axg`), so the row constrains future work — any member ever exhibited must have this value if AMP holds — rather than supplying evidence now.  Originally named at theory/verdicts/soft-index-r1.md F1(c)(i).  Statable only relative to D24(b)'s convention: a change of normalisation rescales `𝔞_leg`, so before that convention the statement had no truth value (bd `tns-iu5` r2 critic F1).  **Quantitative fence (theory/verdicts/d24d3-adjudication-r3.md ⟨1⟩4; one-step form theory/verdicts/d24d3-adjudication-r4.md §1.3):** the leg-conversion factor is computed exactly — `Q^-_k\|Ω⟩ = √(Z_ρ)\|k⟩`, D24N-C8 — and is the exact mismatch, IN THE LSZ LIMIT, between clause (d)2's charge-created residue normalisation and (b)'s asymptotic leg (at finite `N` the descendant-leg ratio is `√(Z_ρ − 2/N)`, D24N-C8(ii)), so a leg normalisation alone supplies only `Z_ρ^{-1/2}`.  That value is refuted against the ansatz-free data ONLY UNDER TWO HYPOTHESES, NEITHER PROVED: (i) the charge-created reading of `E^O_desc` (the clause-1 descendant term is exactly the propagated charge-created leg, with no further dressing), and (ii) the jet-identification bridge (α) — the same bridge D24-VAL carries as a displayed antecedent, without which a two-magnon phase slope and a D24 amputation constant are not comparable at all.  Under both, the refutation is at the PRE-REGISTERED 0.08 band (`--red-halfpower`, deviations 0.42/0.73/1.00 at `S = 1, 3/2, 2`, margins 5.2–12.5×; that red mode's own message quotes the tighter runtime-derived band 0.0479, and the refutation needs neither of them to be tight).  AMP is therefore true only if a second factor `Z_ρ^{-1/2}` arises from a mechanism that is NOT a leg normalisation; any proposed proof must be checked against D24N-C8 or it double-counts the leg. | CONJECTURE | D24(b), D24(d) | — | theory/checks/d24d3_normalization_check.py D24N-C8 (the leg-conversion constant, vacuum and descendant legs); D24N-C6 `--red-halfpower` (the leg-normalisation-only value refuted) |
```

* **5.3d — REPLACE the row beginning `| ML5-A |`: r3 verbatim** (ruled MERGE
  by two critics; no M3 append is owed — this row's where-proved column cites
  `ml5-universality.md ⟨1⟩2` and the `corpus-r2.md` adjudication, neither of
  which any round has superseded).

### 5.4 `notation.md` (the L4 symbol registry)

**5.4a′ — ADD immediately after the replaced `𝒮_W` row: r4 verbatim** (the
`𝔞_leg(ρ)` row carries M1-of-r3's kernel-existence scope; the r4 critic ruled
all four rows MERGE and applied them):

```
| `L(k,h)`, `L(0,h)` | D24(d)3a external flux factor: the descendant quotient `E^O_desc/[(e^{ik}−1)·2iv_h·M_1^O(h)]`, defined for `k ≠ 0` where `M_1^O(h) ≠ 0`; `L(0,h)` is the value at `k=0` of the hypothesised `C¹` extension.  D24(d)3b asserts its `h`-profile — the reciprocal LSZ energy denominator, whose provenance in the corpus (`soft-current-recon.md` ⟨1⟩7; `paper/main.tex:524–527`; `soft-index-b.md:808`) is written in the D6 register `ρ = 1/2` and fixes the profile, not the constant.  `L(0,h) = 𝔞_leg(ρ)·(−iχ(h,0)/v_h)`.  Normalisation-dependent: `L ↦ (c′/c)L` under D24(b)'s rescalings | definitions.md D24(d)3 |
| `Z_ρ` | order-parameter density `2ρ`; for an su(2) model with a fully polarised α tail, `Z_ρ = ω_α([S^+_x, S^-_x])`.  Subscripted deliberately: bare `Z` stays Pauli-reserved (overload rule 9, unchanged) | definitions.md D24(d) |
| `𝔞_leg(ρ)` | soft-leg amputation constant of D24(d)3b, `𝔞_leg(ρ) := i v_h L(0,h)/χ(h,0)`; a datum of the class `𝒮_W(ρ)`, defined only when that class is nonempty AND only in a register where the D24(b) asymptotic one-magnon kernel is constructed (currently the fully polarised spin-`S` family, D24N-C8; off it, kernel existence is claim ML1, CONJECTURE); constant in `k`, `h` and `O` but NOT in `ρ`.  Meaningful only in the D24(b) normalisation convention, which rescales it.  Value OPEN: conditionally matched `= 1/Z_ρ` by claim D24-VAL; asserted `= 1/Z_ρ` by the CONJECTURE row AMP (scoped, without the bridge); `= 1` as frozen (withdrawn).  Written bare `𝔞_leg` inside a fixed-`ρ` display (overload rule 13) | definitions.md D24(d)3b |
| `E^O_desc` | descendant external-leg term of the D24(d)1 decomposition (fraktur `𝔈` avoided: `𝔈_W`, `𝔈_{A_α}`, `𝔈_{A_β}` are the window CP maps); not a transfer map — see overload rule 8 | definitions.md D24(d)1 |
```

**5.4b″ — REPLACE the `𝒮_W` row — REVISED (r4-critic m2: `HANDOFF.md` added
to the licensing clause's enumerated list as the nineteenth live site):**

```
| `𝒮_W(ρ)` | the ρ-indexed five-condition Ward/LSZ/no-contact source class in the repaired conditional ML5-B implication; `ρ := ω_α(S^z) > 0`.  Written bare `𝒮_W` where the density is fixed or immaterial — the D24 section header, D25, the CLAIMS rows quantifying over the class (`S-general` and the corner-implication prose), `ml5-universality.md`, `HANDOFF.md`, and the Letter (`\mathcal{S}_W`) | definitions.md D24(d) |
```

* **5.4c — the `𝖲(k_s;{q_i,v_i})` row: r3 verbatim** (ruled MERGE; applied
  as the one-string replacement in the Mechanics list).
* **5.4d — overload rule 2 amendment: r3 verbatim** (ruled MERGE).
* **5.4f — new overload rule 13: r3 verbatim** (ruled MERGE; 13 confirmed the
  next free number, X6, re-verified live this round).

**5.4e′ — APPEND to overload rule 8 — the FENCED TEXT IS r3 VERBATIM and
unchanged; only the rationale's live-site address is corrected (r4-critic
m2):**

*Rationale (not merged text).*  `E_O` is live at `definitions.md:67`,
`theory/spt-rebuild.md:183`, and `theory/ansatz-scattering-2m.md` at the line
``  `Ψ_O(Y):=tr_{d^{w_O}}[(O⊗1)V_{w_O}YV_{w_O}^†]=E_O(Y)`. `` — **hint
`:170`, re-verified live this round**.  r2 wrote `:141`; r3 corrected it to
`:146`; `:146` is now a `**⟨2⟩4. QED.**` line, and the string above is the
durable address.  So `E^O_desc` collides with rule 8 as written, and the
appended exception is required:

```
   Exception (bd `tns-iu5` r3): `E^O_desc`, with the literal word subscript
   `desc` and an operator superscript, is the D24(d)1 descendant
   external-leg term and is NOT a transfer map or a contraction of one.  It
   is the only member of the `E` family carrying a word subscript.
```

### 5.5″ ADVISORY — same-commit obligations (NOT optional; fourteen items, two re-addressed)

Items 1–7, 9–11, 13–14 are r3/r4 verbatim and are re-verified live this round
(§4.3, Y11).  Two items are re-addressed and one is extended:

1. Apply r3 §4.2's `ml5-universality.md` edits (constant `2i𝔞_leg χ`, the
   Wigner sentence, header ERRATUM pointer) in the SAME commit.  Do not ship
   §5.0″–§5.4 alone.
2. Apply r3 §4.2's `TRIANGLE.md` cross-reference refresh — `𝒮_W → 𝒮_W(ρ)` at
   the five sites of §4.3 rows 1–5, matched on their strings.
3. ERRATUM pointers (not edits) at the heads of `theory/soft-index.md` and
   `theory/soft-index-b.md`, pointing at this verdict.
4. ERRATUM pointer at `theory/verdicts/soft-index-r1.md` (hint `:170`) — "for
   every model in the class … phase slope `2χ`" is superseded.
5. Append the r3 annotation to `theory/verdicts/corpus-r2.md` (hint
   `:174–178`), given verbatim in r3 §4.2, so that ML5-B's where-proved
   column does not point at a document certifying a different conclusion.
   **This is the same defect species as M3; §5.3a′/§5.3b′ close the other
   instance of it.**
6. The one-clause scoping edit at `paper/main.tex:524–527` given verbatim in
   r3 §4.2.
7. bd `tns-v44` instruction with r3's and the r3 critic's riders, the latter
   now discharged into displayed text: the shard must carry `𝔞_leg(ρ)` as the
   displayed OPEN constant, must not describe it as *derivable*, must not use
   the leg-normalisation mechanism, and must not assume clause (d)2's residue
   and `E^O_desc` are in the same normalisation — §5.2″ says in displayed
   words that they are not.
8. **(EXTENDED, r4-critic m2)** `HANDOFF.md`'s corpus-damage line — matched on
   the string `**Frozen 𝒮_W empty for` (hint `:40`) through the end anchor
   `tns-iu5, blocks any D29/D30 merge.` — the whole of corpus-damage item (3)
   is replaced, and becomes "*repaired by
   retirement: clause 3 split, D24(b) convention fixed, `𝔞_leg(ρ)` OPEN (rows
   `D24-VAL` conditional, `AMP` CONJECTURE); ML5-B now predicts no number at
   any density; `𝒮_W(ρ)` nonemptiness open at every `ρ`*".  **Record for the
   auditor:** this line is the **nineteenth** live bare-`𝒮_W` site (§4.2); it
   is *both* licensed by §5.4b″'s enumerated list *and* rewritten here, so the
   licence does not depend on this rewrite's exact wording and this rewrite
   does not depend on the licence.
9. Paper-lane ticket (r2-critic n4, outside this lane): `paper/main.tex:179,
   181–182` (Fig-2 caption) — "the **universal** coefficient `2`", "**Every**
   soft magnon…", no `s`/`ρ` qualifier.  Unaffected by this repair (contact
   route); file it, do not fix it here.
10. **(M4)** The nineteen-site rename disposition of §4.3: `TRIANGLE.md`'s
    five sites renamed (item 2); the other **fourteen** are left untouched
    and are legal under §5.4b″'s licensing clause — no edit to
    `definitions.md`, `claims/CLAIMS.md`, `ml5-universality.md`,
    `paper/main.tex` or `HANDOFF.md` **on account of the rename** (item 8
    edits `HANDOFF.md` for a different reason).
11. **(m3)** Apply the `notation.md` edits in the Mechanics order
    `b′ → a′ → c → f → e → d`.
12. **(m4 of r3-critic; ADDRESS CORRECTED, r4-critic m1)** ERRATUM-free
    citation notes at the two `D24(b)` sites, in the same commit:
    * at `theory/ml4-ward-reduction.md`, **matched on the string** "*An
      **ML4-admissible analytic amputation at fixed `N`** may multiply (3)*",
      at the end of that sentence's paragraph (hint `:63–68`, re-verified
      verbatim live), append: "*(bd
      `tns-iu5` r4: D24(b)'s leg-normalisation convention is strictly
      narrower than this ML4-admissible class — D24(b) soft-leg envelopes
      must have `λ(0) = 1`; conclusions drawn here about ML4-admissible
      amputations do not transfer to D24(b) amplitudes unless they respect
      that restriction.)*";
    * at `theory/ansatz-scattering-2m.md`, **matched on the string**
      "*multiplier amputation of D24(b), with no extra `ε^{-1}` factor.*"
      — the last line of the ⟨2⟩5 paragraph, **hint `:692`; do NOT use a line
      number, that file is under concurrent edit by another lane and r3's
      `:614, 621–631` and r4's `:621–631` are both stale** — append: "*(bd
      `tns-iu5` r4: this `S_phys`-valued amputation coincides with the D24(b)
      convention only in this shard's D6 register `ρ = 1/2`; it is not the
      D24(b) convention in general.)*"
13. **(m8)** One-clause scoping edit at `theory/TRIANGLE.md`, **matched on
    the string** `conditional on wave operators (ML1) and the amputation
    convention`, i.e. after the `𝖲(k_s) = 2ik_s` display and its **SKETCH**
    label (hint `:216–218`, re-verified live): append "*(the D24(b) convention, now displayed; in
    this D6 register the coefficient is `2`; on the general class the same
    statement carries `2𝔞_leg(ρ)` with `𝔞_leg(ρ)` OPEN, D24(d)3b)*".
14. **(m6 of r3-critic)** One sentence added to the H8 paragraph when item 8
    updates `HANDOFF.md`: after the merge, for every source with `M_1^O ≠ 0`
    the ML5-A membership test through `𝔠_h` is undecidable until AMP or
    `tns-axg` closes, because `𝔠_h` contains the open constant `𝔞_leg`;
    D24(e)'s counterexamples remain decidable (`M_1^O = 0`, W14).

---

## 6. Certificate, fifth pass (M1, m3, m4, m5)

`theory/checks/d24d3_normalization_check.py`, **795 loc** (L2 disclosure:
295 over the 500-loc guideline; r4 disclosed 727, r3 559.  The r5 increase is
entirely docstring and comment: M1's two-scope explanation, m3's deletion
note, m4's declared blind spot and m5's coverage statement.  The r3/r4 split
candidate — algebra module / data module — stands and is the natural next
refactor; flagged, not hidden, for the fourth round running.)  No bare
`assert`; every failure raises `SystemExit(1)` with the gate name as the
first stderr token.  **No gate was added, removed or re-tuned this round**;
one constant-true `require` was deleted and one DISPLAY line was recomputed.

### 6.1 M1 — the DISPLAY line recomputed against the measured leg, and relabelled

**Red first (L1).**  Before touching the checker, an independent probe was
written — a from-scratch two-quantum-sector construction with its own ladder
coefficients, importing nothing from the repo — that (i) measures
`‖Q^-_0|h⟩‖²` at `2S = 4`, `N = 8`, (ii) forms the residue against a
`δ`-normalised **descendant** leg, and (iii) compares it against every number
the checker's C3 DISPLAY line prints.  It measured
`‖Q^-_0|h⟩‖² = 30.0000000000` (closed form `Z_ρN − 2 = 30`), leg ratio
`1.9364916731` against `√Z_ρ = 2`, and true value **`2.921187`** against the
printed `2.828427` — **`3.28 %`**, reproducing the r4 critic's table exactly
from an independent construction.  **The probe exited 1 against r4's
checker.**

**The repair.**  `check_ward_residue_scaling` now returns three display
quantities instead of two, and the finite-`N` one is computed by dividing by
the **measured** leg ratio `‖Q^-_0|h⟩‖/√N` — the vector is already built in
that function, so the printed number is a measurement and not a restatement
of C8's closed form.  The banner line names both scopes explicitly.  **The
probe now exits 0.**  Both of the critic's offered options were taken rather
than one, because a DISPLAY that prints one number admitting two readings is
precisely what created M1.

Both lines remain **DISPLAY, never gated**: as gates they would be
deductively subsumed by C3 ∧ C8, which is the r4 disposition and is not
reopened.

### 6.2 m3 — the constant-true `require`, deleted

`derive_exponent_band`'s `require(band > 0.0, …)` could not fail on any
input: by `derive_accept_band`'s own guards `accept_band ∈ (0, 0.08)`, and
`ratio = Σlog(2S)/Σlog²(2S)` is the fixed positive constant `0.880539` over
the module-level `SPINS`, so `band > 0` always.  Re-probed this round at
`accept_band = 1e-12, 1e-6, 1e-3, 0.0479, 0.0799`: `8.8062e-13, 8.8054e-07,
8.8098e-04, 4.3221e-02, 7.3325e-02` — strictly positive by construction,
reproducing the critic's probe.  **Deleted**, with the reason in a code
comment and in the docstring's constants inventory, which now states that
**no constant-true `require` remains in the file**.  The critic's framing is
recorded because it is the right one: a round that deleted a guard for being
constant-true shipped a new one.

### 6.3 m4 — the corrupted-entry guard's structural blind spot, declared

`ROW_BAND_SANITY` tests `max/median`, a **ratio**, hence invariant under
multiplying every `error` entry by a constant.  Verified this round on a
copy: the whole error column `×1.6` **and** every slope shifted `−6 %` →
**exit 0, green**.  A `−6 %` corruption of the ground truth passes every
gate.  r4 §6.4's residual covered "a corrupted entry inflated by less than
`3×` the median" and did not state that the guard is structurally blind to
the **uniform** channel.  Now stated, in `derive_accept_band`'s docstring and
in the module constants inventory: *the guard is a ratio and cannot see a
uniform inflation of the error column; the only backstop there is the
`band < DECISION_BAND` subsumption guard.*  **Nothing is exposed** — both
refutations hold at the pre-registered `0.08` with `5–37×` margins and need
no derived band at all (X2), and `m8_errinflate` (`×4`) is still caught by
the subsumption guard (re-run this round: dies with `derived band 0.1914`).
No new guard is invented for this channel: a guard that could see it would
have to know the true error scale, which is exactly the constant the M2
repair removed.

### 6.4 m5 — sub-gate (v)'s isolated error, reported

The critic's three measured facts are accepted: (v) is insensitive at
`2S = 1` (the coincidence term carries `2S − 1 = 0`); under `--red-ladder`
C2's reported max is set by sub-gate (iii), so **no red mode's exit path ever
names (v)**; and every `ladder_element` mutation is already caught exactly by
sub-gate (ii)'s dense cross-check, so (v)'s independent content is the
**composition path** (`apply_charge ∘ apply_charge`, `inner`, the coincidence
bookkeeping).

The first of the critic's two offered fixes is taken — report the isolated
error beside C2's max — and **not** the second (give (v) its own `require`),
for a stated reason: a second `require` inside C2 would change
`--red-ladder`'s message and break the byte-preservation of the six r3 red
paths that X4 and Y2 fence.  The banner's C2 line now carries
`sub-gate (v) alone … error=…`, and the code comment and docstring both state
that **(v) adds composition-path coverage, not ladder coverage.**  Measured
this round: isolated (v) error `1.199e-13` in green (it **is** the tightest
sub-gate — C2's reported max is (v)'s error), and `2.436e+03` under
`--red-ladder`, with the `2S = 2`, `N = 6` mutated overlap `+280.560` against
the predicted `−24` — reproducing the critic's `2.44e+03` and r4 §6.2's
number.

### 6.5 Green banner, red modes, mutants

**Green** (`python3 -O`, exit **0**):

```
D24N-C1 GUARD frozen_jet_coefficient=2*chi matched=chi/rho
D24N-C2 ladder x4 (residue, dense/occupation cross-check, multi-quantum norms, soft-leg norm + two-magnon same-momentum overlap = -2N*Z_rho), max_error=1.199e-13; sub-gate (v) alone (the two-magnon overlap, composition-path coverage) error=1.199e-13
D24N-C3 Ward residue = Z_rho*2iJ sin h against the CHARGE-CREATED leg, max_error=4.814e-15
D24N-C3 DISPLAY both normalisations at 2S=4, h=pi/4 (gates nothing; deductively C3 / C8): charge-created |residue|=5.656854 (= Z_rho*2J sin h); against (b)'s asymptotic leg the same residue is 2.828427 (= sqrt(Z_rho)*2J sin h) in the LSZ LIMIT, and 2.921187 at this finite N, where the measured descendant-leg ratio is sqrt(Z_rho - 2/N), not sqrt(Z_rho) (C8(ii)); the two normalisations coincide only at Z_rho = 1
D24N-C4 ansatz-free slopes vs 1/S: max_rel_dev=0.0043 (pre-registered band 0.08)
D24N-C5 DISPLAY frozen-clause relative deviations at S=1/2,1,3/2,2: 0.000, 1.002, 2.003, 3.003  -> defect size 3.003
D24N-C6 candidate max_rel_dev=0.0033 vs acceptance band 0.0479 DERIVED from the data's own quoted error (< pre-registered 0.08, so C6 is not subsumed by C4); symbolic (2S)^p=2S root p=1 at S=1,3/2,2; S=1/2 degenerate: 1**p - 1 = 0
D24N-C6 DISPLAY fitted exponent=1.0024 vs 1, derived exponent band 0.0432 (0.8805*log(1/(1-band)), same error column; gates nothing -- at calibrated bands this comparison is deductively subsumed by the acceptance gate, proof in derive_exponent_band; r3's 0.03 gate did independent work only because it was miscalibrated)
D24N-C6 resolving intervals for p: at the derived band [0.969, 1.037]; at the pre-registered band [0.947, 1.062]; at r2's retired 0.02 band [0.989, 1.017] (reported, gates nothing)
D24N-C7 DISPLAY N-dependent half-power route gap=1.855e-01
D24N-C8 vacuum leg Q^-_k|Om> = sqrt(Z_rho)|k>: eigenvector residual=1.799e-15, overlap error=8.882e-16; descendant leg ||Q^-_q|h>||^2 = Z_rho*N - 2: error=7.105e-15 (so the conversion is sqrt(Z_rho) exactly on the vacuum, and only in the LSZ limit on the descendant leg); leg conversion alone gives a_leg = Z_rho^(-1/2), refuted by --red-halfpower at the PRE-REGISTERED band
PASS: D24-VAL candidate a_leg = 1/(2 rho) matches; frozen a_leg = 1 and leg-conversion a_leg = Z_rho^(-1/2) both refuted; the D24(d)3b definition itself fixes no value (lemma AMP open)
```

Every r4 quantity reproduces unchanged: C2 `1.199e-13`, C3 `4.814e-15`, C4
`0.0043`, C5 deviations, C6 `0.0033` vs `0.0479`, exponent band `0.0432`,
both resolving intervals, C7 `1.855e-01`, C8 `1.799e-15 / 8.882e-16 /
7.105e-15`.  The two changed lines are C2 (the isolated `(v)` error appended)
and C3 DISPLAY (the two scopes).

**Red modes — all seven re-run, exit PATHS and numbers BYTE-PRESERVED:**

| mode | exit | died at (first stderr token) |
|---|---|---|
| `--red-frozen` | 1 | **D24N-C6** `at S=1.0 dev=1.0016; S=1.5 dev=2.0027; S=2.0 dev=3.0033` |
| `--red-power` | 1 | **D24N-C6** `at S=1.0 dev=0.4984; S=1.5 dev=0.6640; S=2.0 dev=0.7467` |
| `--red-halfpower` | 1 | **D24N-C6** `at S=1.0 dev=0.4158; S=1.5 dev=0.7347; S=2.0 dev=1.0033` |
| `--red-ward` | 1 | **D24N-C3** `Ward residue Z-scaling error 1.697e+01` |
| `--red-ladder` | 1 | **D24N-C2** `ladder/soft-leg-norm error 1.477e+06` |
| `--red-legfactor` | 1 | **D24N-C8** `leg-conversion factor is not sqrt(Z_rho) (error 5.657e+00)` |
| `--red-descnorm` | 1 | **D24N-C8** `descendant-leg norm is not Z_rho*N - 2 (error 2.000e+00)` |

**Data mutants re-run this round** (copies in the scratchpad; the DATA
falsified).  Every X13/Y6-certified behaviour is preserved:

| mutant | exit | died at |
|---|---|---|
| `m6_shift30` (`−3 %`) | **0 (passes, and must — X13)** | inside the quoted error |
| `m_shift34`, `m_shift40` | **0 (pass — X13/Y6)** | inside the quoted error |
| `m_shift45` (`−4.5 %`) | 1 | **D24N-C6** acceptance `S=2.0 dev=0.0482` |
| `m2b_shift60` (`−6 %`) | 1 | **D24N-C6** acceptance `S=0.5 dev=0.0604` |
| `m2_shift799` (`−7.99 %`) | 1 | **D24N-C4** `0.0839` |
| `m_errwide` | 1 | **D24N-C6** `worst row band 0.0790 is 3.78x the median row band 0.0209 (guard 3.0x)` |
| `m8_errinflate` (`×4`) | 1 | **D24N-C6** `derived band 0.1914 … C6 would be subsumed by C4` |
| **`errscale16_shift60`** (the r4 critic's m4 mutant: error column `×1.6`, slopes `−6 %`) | **0 (passes — the declared blind spot, §6.3)** | — |

**Gate audit (supersedes r4 §6.5 only where it differs):** unchanged, plus —
C2's sub-gate (v) is now reported in isolation and its coverage claim is
stated at composition-path strength (§6.4); the C3 DISPLAY line is two lines
of one DISPLAY and both are subsumed by C3 ∧ C8; `derive_exponent_band`
contains no `require` at all; and the file's declared-constants inventory now
carries m4's structural blind spot.  **The file contains no constant-true
`require`.**

---

## 7. HONEST STATUS — deltas on r4's H1–H9 (all else unchanged)

**H1 (PROVED) — unchanged.**  `Q^-_k|Ω⟩ = √(Z_ρ)|k⟩` exactly on the vacuum
leg; `‖Q^-_q|h⟩‖² = Z_ρN − 2` exactly on the descendant leg.  r5 adds no
identity and re-derives none.  What it adds is that the **second** identity
is now the one the L4 text and the certificate's DISPLAY both quote when they
speak about the descendant leg — which is what M1 was about.

**H2″ (CONVENTIONAL) — one word.**  The D24(b) convention has two anchors:
the `δ`-normalised asymptotic soft leg for `M_2^O` (§5.0″) and the
charge-created normalisation of clause (d)2's residue display (§5.2″ clause
2).  The conversion between them is `√Z_ρ` **in the LSZ limit**, and
`√(Z_ρ − 2/N)` at finite `N` on the descendant leg; both are displayed, and
`(b)` alone — not clause (d)2 — is what pins `𝔞_leg` against the covariance
freedom, clause (d)2 being a membership condition (m8).  Everything else in
r4's H2′ stands, including the scope to the fully polarised spin-`S` family
and `ML1` off it.

**H7′ (EVIDENCE, not proof) — unchanged, plus one disclosure.**  The
acceptance band is derived at runtime; the exponent comparison is DISPLAY by
subsumption; the two refutations need neither and hold at the pre-registered
`0.08`.  Added: the corrupted-entry guard is structurally blind to a uniform
inflation of the error column (§6.3), which is a statement about the
certificate's reach and belongs in the honest-status list, not only in a
docstring.

**H8 — unchanged** (the `𝔠_h`-undecidability sentence stands, §5.5″ item 14).

**H10 (NEW) — the refutations are conditional and the L4 text now says so.**
`Z_ρ^{-1/2}` is refuted **only** under (α) and the charge-created reading of
`E^O_desc`.  This was always r4's §1.3/§1.4 position; what changed is that it
is now stated where the claim is made — in `definitions.md` clause 3b and in
the `AMP` row — and not only in a verdict file.  The corresponding weakening
is real and is recorded as such: the merge no longer ships the sentence
"`Z_ρ^{-1/2}` is refuted" without hypotheses anywhere.

**H9 — unchanged.**

---

## 8. Disposition

* **Merge**: §5.0″ + §5.1 + §5.2″ + §5.3(a′,b′,c″,d) + §5.4(a′,b″,c,d,e′,f)
  in **one commit**, `definitions.md` spans in descending order by string
  anchor, `notation.md` edits in the order `b′ → a′ → c → f → e → d`,
  `claims/CLAIMS.md` edits matched on row label, together with §5.5″'s
  fourteen same-commit obligations.  Every span the r4 critic ruled MERGE is
  untouched; every HOLD span is re-issued with exactly the demanded repair;
  the four corrected addresses are in the merge text.  **To this lane's
  knowledge nothing now blocks the commit.**
* bd `tns-iu5`: **stays OPEN**, at the r4 critic's four sub-tasks:
  1. *(done, r3+r4)* D24(b)'s amputation and soft-leg convention, scoped to
     the register where its kernel is constructed;
  2. *(done, r5)* clause (d)2's residue normalisation fixed in displayed
     text, clauses 1–3 made consistent with it, and the LSZ/finite-`N` scope
     of the conversion displayed — **this is what closes with this commit**;
  3. *(OPEN — this is `ML1`)* supply, or scope away, the `δ`-normalised
     asymptotic one-magnon kernel off the fully polarised family;
  4. *(OPEN — the prover target)* prove or refute **AMP**, with the fence as
     now worded: leg conversion supplies exactly `Z_ρ^{-1/2}` in the LSZ
     limit, on the charge-created reading of `E^O_desc`, so a second,
     non-leg-normalisation factor of the same size is required; `D24-VAL`
     stays vacuous-or-unknown until (4) closes or `tns-axg` exhibits a
     member.
* bd `tns-axg`: **stays OPEN and may not move.**  `𝒮_W(ρ)` nonemptiness
  remains open at every `ρ`, `ρ = 1/2` included — and open rather than
  provably empty, which the r4 critic confirmed is a real gain.  Nothing here
  exhibits a member.
* bd `tns-v44`: unblocked on this axis; §5.5″ item 7's riders stand.
* **The next prover target is AMP**, unchanged.

**Register comparison** (per protocol).  Nearest PROVED row: `S2-2body-S`,
whose adjudicated scoping sentence is the corpus standard — say exactly what
is proved, then enumerate by name what is not.  This document's claims are:
three sentence-level defects repaired in the merge text and four addresses
corrected; the certificate's one wrong DISPLAY recomputed against a measured
quantity; one constant-true guard deleted; two reach limitations declared.
**NOTHING promoted, no member exhibited, no value installed, no status
changed, no new gate, no new symbol.**  `ML1`, AMP, (α) and nonemptiness are
all still open and all still named at every point of use — and, after M2, at
two points of use where they previously were not.

**Lane-hygiene note for the orchestrator.**  This lane edited only its three
files and made no commit.  However, while r5 was being written another lane's
commit `5349ee5` swept the working-tree state of
`theory/checks/d24d3_normalization_check.py` into itself, so **the checker
half of this round is already in `HEAD`** and must not be re-applied; the
`definitions.md` / `notation.md` / `claims/CLAIMS.md` half of §5 is
unaffected and is still owed as one commit.  The merge was re-applied to
scratch copies of the **post-`5349ee5`, post-`137f922`** files after that
happened, and all thirteen edits still apply with every string anchor
holding.

**Trajectory.**  r1 `3/7/6/4` → r2 `1/5/8/5` → r3 `1/5/8/4` → r4 `0/3/8/5` →
r5: every r4 item addressed — **14 FIXED, 2 RECEIVED with no action
required, 0 deviations, 0 declined**
(disposition table in `theory/verdicts/d24d3-repair-r5-response.md`).  The
physics is unchanged for the fifth consecutive round.  What changed in the
text that ships: one scope word, two restored hypotheses, two DAG pointers,
four addresses, and an anchoring policy that makes the next round's addresses
survive the next lane's edits.

---

## 9. The four questions the r4 critic answered, and what r5 leaves of them

1. **Is the two-anchor convention well posed?**  The critic answered **yes**
   and carried the `(c, c′)` algebra through the (3a) quotient with the
   hard-leg factor explicit (Y12).  r5 does not reopen it and adds nothing to
   it; it only fixes the two places where the *description* of the structure
   was wrong (m7, m8) and the one place where the conversion's scope was
   overstated (M1).  **The structure is the critic's, not this round's.**
2. **Does the Q-identity narrowing survive, and is the discharge
   legitimate?**  **Yes and yes** — §2 of the r4 critic verdict, fenced,
   unreopened.  M2 does not weaken the narrowing; it restores the two
   hypotheses the narrowing was always stated under in r4 §1.3 and §1.4, and
   ships them into the L4 text and the DAG row.
3. **The checker.**  Both standing complaints stay discharged.  This round's
   residuals are closed or declared: the DISPLAY line recomputed (M1), the
   constant-true `require` deleted (m3), the guard's uniform-channel blindness
   declared (m4), and sub-gate (v)'s isolated error reported with its coverage
   claim stated at composition-path strength (m5).  The m2 deviation is
   **accepted** and is not revisited.
4. **Merge appliability.**  The critic verified it by application (Y9) and
   found the ordering correct.  r5 changes no span boundary, no order and no
   edit count: it changes text **inside** two `definitions.md` blocks, text
   inside three `claims/CLAIMS.md` rows, one clause of one `notation.md` row,
   and four addresses in the advisory and rationale prose.  Thirteen edits,
   the same thirteen, in the same order, now addressed by string.
