<!-- ROLE: orchestrator adjudication of the definitional audit (TJO directive
     2026-08-30: "I am suspicious of the definitions... connect to 3+1 QED on
     the lattice"). Lanes: theory/lanes/reduction/q1-gauss.md,
     q2-memory-defs.md, q3-soft-defs.md, q4-adversarial-defs.md (q4 hostile,
     run blind to q1-q3). Plus the earlier result-level lanes r1/r3/r2r4 and
     docs/reduction-limits.md. This file is the single record (PRD one-record
     rule); bd tns-1gf tracks it. -->

# Adjudication r1 — the definitional audit against 3+1 lattice QED

## 0. Cross-lane consistency

Four lanes, one of them (Q4) hostile and run blind to the others, concur on
every overlapping item: the audit is internally consistent. No lane changed
any claim status; none needed changing — the L5 statuses were already honest.
What the audit changes is the permissible NAMING and the continuum-facing
framing.

## 1. Findings adjudicated as GENUINELY NEW (accepted)

- **N1 (Q4, FATAL to a generic-memory reading; Q2 concurs).** D26+D27 define
  integer charge-transfer full-counting statistics. A free Maxwell radiative
  sector can carry a nonzero soft endpoint shift while the matter-charge TPM
  law is δ₀; replacing the observable by the field shift destroys (INT).
  Therefore the package is NOT a definition of generic electromagnetic/
  gravitational radiative memory. Missing bridge: a reconstruction theorem
  tying the charge ledger to the field-side (bond/potential) memory — the
  object docs/framing.md always said memory should live in. → bd (new,
  reconstruction theorem).
- **N2 (Q4, FATAL to a QED reading of D4; Q1 concurs in detail).** The
  finite-χ bond-implementer algebra has the wrong quotient, locality,
  boundary multiplicity, and spectrum for the compact-QED boundary Gauss
  algebra; no limit sequence inside D4 repairs this. The one QED-facing
  statement that survives, and it is exact at finite spacing: (WI) matches
  the constant-profile Gauss telescope on the physical subspace, with
  Q_∂[ε] = Q_H[ε] + Q_∇[ε] → Q_hard + Q_soft as a [physical argument]
  (q1-gauss.md §3.5).
- **N3 (Q4, sharpening of a known fence).** D24(d)'s class conditions
  substantially restate the desired factorization (bootstrap), so ML5-B —
  already recorded as a conditional implication with membership open — must
  additionally never be described as a (nonvacuous) continuum-style soft
  theorem. Raises the stakes of tns-axg (exhibit a member) from
  "nice-to-have" to "framing-critical".
- **N4 (Q2/Q3).** The fixed-time protocol datum (D29, PROPOSED) and the
  accepted LSZ soft-leg definition (which D24(b) correctly is) coincide with
  zero remainder ONLY on the separated-preparation class (the proved
  matching row); otherwise they differ at O(k). The proto-LSZ gap is
  therefore a DEFINITIONAL gap, not merely a technical one.
- **N5 (Q4 MAJOR).** D13(a) admits a charged-dressing masquerade (core
  charge/profile changes read as translation) unless a fixed-core centering
  condition is added; D13(c)'s displayed coordinate is exactly conserved
  (the recorded trap), and D18 does not itself define the leg-subtracted
  dress. A centering amendment to D13(a) is owed. → bd (new, D13 hygiene).

## 2. Findings adjudicated as ALREADY-FENCED (no new obligation beyond naming)

- 𝒮_W nonemptiness open (tns-axg), D24-VAL/AMP vacuous-or-unknown: recorded.
- The D13(c) conservation trap: recorded in the definition itself.
- Ansatz-vs-model gap (D5/D12 are technology, not a particle definition):
  brief §5.2; exactness enters only via D28/D31 hypotheses, as the rows say.
- No Weinberg pole without a gauge mediator: brief §5.1; Q3's control
  experiment CONFIRMS the definitions distinguish the cases correctly (the
  photon pole lies outside the regular Taylor class; a matter-charge
  insertion does not fake it).

## 3. What SURVIVED the hostile hunt (for the record)

Every definition is internally exact lattice mathematics (Q4 §6.2): D13(a)
narrowly as a centered rigid-soliton collective coordinate; D13(b) as exact
endpoint-jump/Fourier-residue kinematics; D26(INT) as the correct compact
U(1)/c=1 rotor arithmetic (clause-for-clause match with compact lattice QED
integer electric fields, Q2); D27 renamed as conditional TPM/FCS; D24(a)-(c)
incl. the accepted LSZ leg normalisation; D4 internally as the finite-MPS
endpoint construction; (WI) as the Gauss telescope match. The magnetic
memory NAME is justified within magnetism: δx = −N_T/s reduces CLEANLY,
constants included, onto accepted magnonics domain-wall motion
(r3-wall.md / reduction-limits.md R3a). The forbidden step is only the
generic radiative-memory reading (N1).

## 4. Actions

1. Letter/skeleton NAMING DISCIPLINE (TJO decision required): the safe
   names are "collective-coordinate charge ledger", "finite-window Fourier
   response", "compact-charge TPM statistics", "conditional exact-band
   scattering"; "memory" is defensible in the magnonics sense only; no
   generic radiative-memory or QED-boundary-charge language without the
   missing bridges. → bd (new, framing decision).
2. Reconstruction theorem (charge ledger ↔ bond/potential-side memory
   field) → bd, P1. The Giulini framing note predicted its shape.
3. D13(a) centering amendment → bd, P2 (definition hygiene, lockstep with
   labbook shard 5).
4. Labbook shard 18 + docs/reduction-limits.md absorb the four Q-lane
   verdicts (integration lane dispatched 2026-08-30).
5. No claim status changes: audited and none required.
